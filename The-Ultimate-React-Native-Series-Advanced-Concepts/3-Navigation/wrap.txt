*** 1- Introduction ***
# 1. Overview of the Lecture

This section introduces **navigation in mobile apps**, focusing on making the app more interactive and functional.

Key idea:
Move from a static UI to a **multi-screen app** where users can:

* Switch between screens using tabs
* Open detailed views from list items (cards)
* Interact with gestures (e.g., pull-down)

---

# 2. What is Navigation?

Navigation allows users to move between different parts (screens) of an app.

### Common navigation patterns:

1. **Tab Navigation (Bottom Tabs)**
   Switch between main sections of the app.

2. **Stack Navigation**
   Move forward to a new screen (e.g., details) and go back.

3. **Gesture-based Navigation**
   Interactions like swipe or pull-down.

---

# 3. Step 1 – Setting Up Navigation

Install required libraries (React Native example using React Navigation):

-------------------code----------------------
npm install @react-navigation/native
npm install react-native-screens react-native-safe-area-context
npm install @react-navigation/bottom-tabs
npm install @react-navigation/stack
-------------------code----------------------

---

# 4. Step 2 – Create Navigation Container

The navigation container wraps the entire app.

-------------------code----------------------
import React from "react";
import { NavigationContainer } from "@react-navigation/native";

export default function App() {
  return (
    <NavigationContainer>
      {/* Navigators go here */}
    </NavigationContainer>
  );
}
-------------------code----------------------

---

# 5. Step 3 – Implement Bottom Tab Navigation

This creates tabs at the bottom of the screen.

-------------------code----------------------
import { createBottomTabNavigator } from "@react-navigation/bottom-tabs";

const Tab = createBottomTabNavigator();

function FeedScreen() {
  return null;
}

function SettingsScreen() {
  return null;
}

function Tabs() {
  return (
    <Tab.Navigator>
      <Tab.Screen name="Feed" component={FeedScreen} />
      <Tab.Screen name="Settings" component={SettingsScreen} />
    </Tab.Navigator>
  );
}
-------------------code----------------------

---

# 6. Step 4 – Implement Stack Navigation (Details Screen)

Used when tapping a card to open details.

-------------------code----------------------
import { createStackNavigator } from "@react-navigation/stack";

const Stack = createStackNavigator();

function ListingsScreen({ navigation }) {
  return (
    // Example card press
    <Button
      title="Go to Details"
      onPress={() => navigation.navigate("Details")}
    />
  );
}

function ListingDetailsScreen() {
  return null;
}

function FeedNavigator() {
  return (
    <Stack.Navigator>
      <Stack.Screen name="Listings" component={ListingsScreen} />
      <Stack.Screen name="Details" component={ListingDetailsScreen} />
    </Stack.Navigator>
  );
}
-------------------code----------------------

---

# 7. Step 5 – Combine Tabs with Stack

Each tab can have its own stack.

-------------------code----------------------
function Tabs() {
  return (
    <Tab.Navigator>
      <Tab.Screen name="Feed" component={FeedNavigator} />
      <Tab.Screen name="Settings" component={SettingsScreen} />
    </Tab.Navigator>
  );
}
-------------------code----------------------

---

# 8. Step 6 – Adding Gesture Interaction

Example: Pull-down interaction using ScrollView.

-------------------code----------------------
import { ScrollView, RefreshControl } from "react-native";
import React, { useState } from "react";

function ListingsScreen() {
  const [refreshing, setRefreshing] = useState(false);

  const onRefresh = () => {
    setRefreshing(true);
    // simulate reload
    setTimeout(() => setRefreshing(false), 2000);
  };

  return (
    <ScrollView
      refreshControl={
        <RefreshControl refreshing={refreshing} onRefresh={onRefresh} />
      }
    >
      {/* Content */}
    </ScrollView>
  );
}
-------------------code----------------------

---

# 9. Full Final Code

-------------------code----------------------
import React, { useState } from "react";
import { NavigationContainer } from "@react-navigation/native";
import { createBottomTabNavigator } from "@react-navigation/bottom-tabs";
import { createStackNavigator } from "@react-navigation/stack";
import { Button, ScrollView, RefreshControl, Text } from "react-native";

const Tab = createBottomTabNavigator();
const Stack = createStackNavigator();

function ListingsScreen({ navigation }) {
  const [refreshing, setRefreshing] = useState(false);

  const onRefresh = () => {
    setRefreshing(true);
    setTimeout(() => setRefreshing(false), 2000);
  };

  return (
    <ScrollView
      refreshControl={
        <RefreshControl refreshing={refreshing} onRefresh={onRefresh} />
      }
    >
      <Button
        title="Go to Details"
        onPress={() => navigation.navigate("Details")}
      />
    </ScrollView>
  );
}

function ListingDetailsScreen() {
  return <Text>Details Screen</Text>;
}

function SettingsScreen() {
  return <Text>Settings Screen</Text>;
}

function FeedNavigator() {
  return (
    <Stack.Navigator>
      <Stack.Screen name="Listings" component={ListingsScreen} />
      <Stack.Screen name="Details" component={ListingDetailsScreen} />
    </Stack.Navigator>
  );
}

function Tabs() {
  return (
    <Tab.Navigator>
      <Tab.Screen name="Feed" component={FeedNavigator} />
      <Tab.Screen name="Settings" component={SettingsScreen} />
    </Tab.Navigator>
  );
}

export default function App() {
  return (
    <NavigationContainer>
      <Tabs />
    </NavigationContainer>
  );
}
-------------------code----------------------

---

# 10. Important Notes & Best Practices

* Keep navigation structure **modular** (separate navigators per feature).
* Use **stack inside tabs**, not the opposite.
* Name routes clearly (e.g., `ListingDetails` instead of `Screen2`).
* Avoid deeply nested navigators unless necessary.
* Pass only required data between screens.
* Use gestures (like pull-to-refresh) to improve UX.

---

# 11. Summary

* Navigation transforms your app from static to interactive.
* Bottom tabs allow switching between main sections.
* Stack navigation enables moving to detail screens.
* These can be combined for scalable app structure.
* Gesture interactions enhance user experience.

Core flow learned:

1. Setup navigation
2. Add tabs
3. Add stack for details
4. Combine everything
5. Add interactions

This is the foundation for building real-world mobile apps.


*** 2- Installing React Navigation ***
# 1. Overview of the Lecture

This lecture explains how to **set up navigation using React Navigation** and introduces the main types of navigators.

Key focus:

* Installing React Navigation (version 5)
* Understanding required dependencies
* Learning the three main navigator types
* Preparing to use **Stack Navigator** and **Tab Navigator**

---

# 2. What is React Navigation?

**React Navigation** is a popular library used in React Native apps to manage navigation between screens.

Official documentation:
[React Navigation](https://reactnavigation.org?utm_source=chatgpt.com)

Important note from the lecture:

* The version used is **v5 (specifically 5.3.2)**
* Version 5 is **different from older versions**, so follow the same version for compatibility

---

# 3. Step 1 – Install React Navigation

Install the core library:

-------------------code----------------------
npm install @react-navigation/native@5.3.2
-------------------code----------------------

---

# 4. Step 2 – Install Required Dependencies

React Navigation depends on additional libraries.

### If using Expo (recommended in this lecture):

-------------------code----------------------
expo install react-native-gesture-handler react-native-reanimated react-native-screens react-native-safe-area-context
-------------------code----------------------

### Explanation:

* Expo ensures compatibility with your current environment
* This avoids version conflicts

---

# 5. Step 3 – Bare React Native Setup (Important Note)

If you are **not using Expo** or you eject later:

-------------------code----------------------
npm install react-native-gesture-handler react-native-reanimated react-native-screens react-native-safe-area-context
-------------------code----------------------

Then:

* Perform additional native setup (linking, configuration)
* Follow official docs for platform-specific steps

---

# 6. Types of Navigators

React Navigation provides **three main navigator types**:

---

## 6.1 Stack Navigator

Used to move between screens like a stack.

### Concept:

* Screens are stacked on top of each other
* You can:

  * **Push** a new screen
  * **Pop** back to previous screen

### Example:

-------------------code----------------------
navigation.navigate("Details");
navigation.goBack();
-------------------code----------------------

### Use case:

* Moving from a list → details screen

---

## 6.2 Tab Navigator

Used to create tabs (usually at the bottom).

### Concept:

* Each tab represents a main section
* Users can switch between them easily

### Use case:

* Home / Settings / Profile tabs

---

## 6.3 Drawer Navigator

Used to create a side menu (drawer).

### Concept:

* Slide from the left side
* Contains navigation links

### Use case:

* Apps with many sections

Note:

* Not covered in this course (left for self-study)

---

# 7. Key Insight

All navigators:

* Have **similar APIs**
* Follow the same structure
* Once you learn one → others are easy

---

# 8. Step 4 – Basic Stack Navigator Example

-------------------code----------------------
import React from "react";
import { createStackNavigator } from "@react-navigation/stack";
import { NavigationContainer } from "@react-navigation/native";
import { Button, Text, View } from "react-native";

const Stack = createStackNavigator();

function HomeScreen({ navigation }) {
  return (
    <View>
      <Button
        title="Go to Details"
        onPress={() => navigation.navigate("Details")}
      />
    </View>
  );
}

function DetailsScreen({ navigation }) {
  return (
    <View>
      <Text>Details Screen</Text>
      <Button title="Go Back" onPress={() => navigation.goBack()} />
    </View>
  );
}
-------------------code----------------------

---

# 9. Step 5 – Wrap with Navigation Container

-------------------code----------------------
export default function App() {
  return (
    <NavigationContainer>
      <Stack.Navigator>
        <Stack.Screen name="Home" component={HomeScreen} />
        <Stack.Screen name="Details" component={DetailsScreen} />
      </Stack.Navigator>
    </NavigationContainer>
  );
}
-------------------code----------------------

---

# 10. Full Final Code

-------------------code----------------------
import React from "react";
import { createStackNavigator } from "@react-navigation/stack";
import { NavigationContainer } from "@react-navigation/native";
import { Button, Text, View } from "react-native";

const Stack = createStackNavigator();

function HomeScreen({ navigation }) {
  return (
    <View>
      <Button
        title="Go to Details"
        onPress={() => navigation.navigate("Details")}
      />
    </View>
  );
}

function DetailsScreen({ navigation }) {
  return (
    <View>
      <Text>Details Screen</Text>
      <Button title="Go Back" onPress={() => navigation.goBack()} />
    </View>
  );
}

export default function App() {
  return (
    <NavigationContainer>
      <Stack.Navigator>
        <Stack.Screen name="Home" component={HomeScreen} />
        <Stack.Screen name="Details" component={DetailsScreen} />
      </Stack.Navigator>
    </NavigationContainer>
  );
}
-------------------code----------------------

---

# 11. Important Notes & Best Practices

* Always use the **same library version** as the course (v5.3.2 here)
* Use **Expo installation** when possible to avoid issues
* Keep navigation structure **simple and modular**
* Learn Stack first → then Tabs → then Drawer
* Avoid mixing too many navigator types early
* Follow official docs for updates and breaking changes

---

# 12. Summary

* React Navigation is the standard library for navigation in React Native
* Installation requires:

  * Core package
  * Supporting dependencies
* There are three navigator types:

  1. Stack (most important)
  2. Tabs
  3. Drawer
* Stack Navigator works like browser navigation (push/pop)
* Expo simplifies setup and dependency management

Next step:

* Learn how to fully implement and customize **Stack Navigator** in real apps

*** 3- Stack Navigator ***
# 1. Overview of the Lecture

This lecture focuses on **implementing Stack Navigation using React Navigation v5** and understanding how navigation works internally.

Key ideas:

* Each navigator (Stack, Tab, Drawer) is a **separate package**
* Build a **Stack Navigator** to move between screens
* Use a **Navigation Container** to manage navigation state
* Define routes using `Stack.Screen`

---

# 2. Key Concepts

### 2.1 Navigator

A navigator controls how users move between screens.

### 2.2 Stack Navigator

* Works like a browser history (push/pop)
* Screens are stacked on top of each other

### 2.3 Navigation Container

* Wraps the entire app
* Manages navigation state

---

# 3. Step 1 – Install Stack Navigator

Each navigator is installed separately.

-------------------code----------------------
npm install @react-navigation/stack@5.3.5
-------------------code----------------------

---

# 4. Step 2 – Create Test Screens

Before integrating into your real app structure, the lecture isolates navigation.

### Example screens:

-------------------code----------------------
import React from "react";
import { Text } from "react-native";
import Screen from "./app/components/Screen";

function TweetsScreen() {
  return (
    <Screen>
      <Text>Tweets</Text>
    </Screen>
  );
}

function TweetDetailsScreen() {
  return (
    <Screen>
      <Text>Tweet Details</Text>
    </Screen>
  );
}
-------------------code----------------------

---

# 5. Step 3 – Create Stack Navigator

### Import and initialize:

-------------------code----------------------
import { createStackNavigator } from "@react-navigation/stack";

const Stack = createStackNavigator();
-------------------code----------------------

### Important:

* `Stack` is an **object with components**

  * `Stack.Navigator`
  * `Stack.Screen`

---

# 6. Step 4 – Define Routes

Create a navigator component:

-------------------code----------------------
function AppNavigator() {
  return (
    <Stack.Navigator>
      <Stack.Screen name="Tweets" component={TweetsScreen} />
      <Stack.Screen name="TweetDetails" component={TweetDetailsScreen} />
    </Stack.Navigator>
  );
}
-------------------code----------------------

### Explanation:

* `name` → unique route identifier
* `component` → screen to render

---

# 7. Step 5 – Wrap with Navigation Container

-------------------code----------------------
import { NavigationContainer } from "@react-navigation/native";

export default function App() {
  return (
    <NavigationContainer>
      <AppNavigator />
    </NavigationContainer>
  );
}
-------------------code----------------------

---

# 8. Understanding Navigation Behavior

### 8.1 Default Screen

* The **first `Stack.Screen`** is shown initially

### 8.2 Changing Initial Screen

-------------------code----------------------
<Stack.Navigator initialRouteName="Tweets">
-------------------code----------------------

---

### 8.3 Header Behavior

* Automatically added in Stack Navigator
* Can be customized:

  * Title
  * Colors
  * Buttons

---

# 9. Applying This to Your Project Structure

You already have a clean structure:

-------------------code----------------------
app/
  screens/
  components/
  navigation/   (you should add this)
-------------------code----------------------

### Recommended improvement:

Create a new folder:

-------------------code----------------------
app/navigation/
  AppNavigator.js
-------------------code----------------------

---

# 10. Step 6 – Real App Integration

### Example using your screens:

-------------------code----------------------
import React from "react";
import { createStackNavigator } from "@react-navigation/stack";
import ListingsScreen from "../screens/ListingsScreen";
import ListingDetailsScreen from "../screens/ListingDetailsScreen";

const Stack = createStackNavigator();

function AppNavigator() {
  return (
    <Stack.Navigator>
      <Stack.Screen name="Listings" component={ListingsScreen} />
      <Stack.Screen name="ListingDetails" component={ListingDetailsScreen} />
    </Stack.Navigator>
  );
}

export default AppNavigator;
-------------------code----------------------

---

# 11. Update App.js

-------------------code----------------------
import React from "react";
import { NavigationContainer } from "@react-navigation/native";
import AppNavigator from "./app/navigation/AppNavigator";

export default function App() {
  return (
    <NavigationContainer>
      <AppNavigator />
    </NavigationContainer>
  );
}
-------------------code----------------------

---

# 12. Full Final Code

## 12.1 app/navigation/AppNavigator.js

-------------------code----------------------
import React from "react";
import { createStackNavigator } from "@react-navigation/stack";
import ListingsScreen from "../screens/ListingsScreen";
import ListingDetailsScreen from "../screens/ListingDetailsScreen";

const Stack = createStackNavigator();

function AppNavigator() {
  return (
    <Stack.Navigator>
      <Stack.Screen name="Listings" component={ListingsScreen} />
      <Stack.Screen name="ListingDetails" component={ListingDetailsScreen} />
    </Stack.Navigator>
  );
}

export default AppNavigator;
-------------------code----------------------

---

## 12.2 App.js

-------------------code----------------------
import React from "react";
import { NavigationContainer } from "@react-navigation/native";
import AppNavigator from "./app/navigation/AppNavigator";

export default function App() {
  return (
    <NavigationContainer>
      <AppNavigator />
    </NavigationContainer>
  );
}
-------------------code----------------------

---

# 13. Important Notes & Best Practices

* Always wrap your app with `NavigationContainer`
* Keep navigators in a **separate folder (navigation/)**
* Use **clear route names** (e.g., `ListingDetails`)
* Follow naming convention:

  * `Stack` → navigator object
  * `AppNavigator` → component
* Avoid putting navigation logic inside screens
* Start simple (Stack only), then scale

---

# 14. Summary

* Stack Navigator allows moving between screens like a stack
* You define routes using `Stack.Screen`
* The app must be wrapped in `NavigationContainer`
* First screen is default unless `initialRouteName` is set
* Navigation structure should be separated from UI

Flow learned:

1. Install stack navigator
2. Create screens
3. Create navigator
4. Define routes
5. Wrap with navigation container
6. Integrate into real app

Next step:

* Navigate between screens using `navigation.navigate()`

Here is the **same lecture code**, but rewritten using **arrow functions only**, without any structural improvements.

---

# App.js (Arrow Functions Version)

-------------------code----------------------
import React from "react";
import { Text } from "react-native";
import { NavigationContainer } from "@react-navigation/native";
import { createStackNavigator } from "@react-navigation/stack";

import Screen from "./app/components/Screen";

const Stack = createStackNavigator();

const Tweets = () => {
  return (
    <Screen>
      <Text>Tweets</Text>
    </Screen>
  );
};

const TweetDetails = () => {
  return (
    <Screen>
      <Text>Tweet Details</Text>
    </Screen>
  );
};

const StackNavigator = () => {
  return (
    <Stack.Navigator>
      <Stack.Screen name="Tweets" component={Tweets} />
      <Stack.Screen name="TweetDetails" component={TweetDetails} />
    </Stack.Navigator>
  );
};

const App = () => {
  return (
    <NavigationContainer>
      <StackNavigator />
    </NavigationContainer>
  );
};

export default App;
-------------------code----------------------

---

# Notes

* Same logic as lecture, only changed:

  * `function Name()` → `const Name = () => {}`
* No shortcuts (kept `return` for clarity like lecture)
* Structure remains:

  * Screens inside same file
  * Stack navigator defined locally
  * App wrapped with `NavigationContainer`

---

*** 4- Navigating Between Screens ***
# 1. Overview of the Lecture

This lecture explains **how to navigate between screens** in React Navigation and how to access the navigation system from both:

* Screen components
* Non-screen (child) components

Key topics:

* `navigation.navigate()`
* Difference between `navigate` and `push`
* Using the **navigation prop**
* Using the **useNavigation hook**

---

# 2. Navigation Between Screens

React Navigation provides a **navigation prop** to screen components.

### Key idea:

* Every screen automatically receives a `navigation` object
* This object allows moving between screens

---

# 3. Step 1 – Add a Button to Navigate

### Inside `Tweets` screen:

-------------------code----------------------
import { Button } from "react-native";

const Tweets = ({ navigation }) => {
  return (
    <Screen>
      <Button
        title="View Tweet"
        onPress={() => navigation.navigate("TweetDetails")}
      />
    </Screen>
  );
};
-------------------code----------------------

### Explanation:

* `navigation.navigate("TweetDetails")`
  → moves user to the **TweetDetails screen**

---

# 4. What Happens Automatically

* A **header** is shown (Stack Navigator default)
* A **back button** appears automatically
* Navigation state is managed internally

---

# 5. Understanding the Navigation Object

The `navigation` object provides methods like:

* `navigate()` → go to a screen
* `goBack()` → go back
* `reset()` → reset navigation state

### Stack-specific methods:

* `push()` → add new screen instance
* `replace()` → replace current screen
* `pop()` → go back one screen
* `popToTop()` → go to first screen

---

# 6. navigate vs push (Important Concept)

### 6.1 navigate()

-------------------code----------------------
navigation.navigate("Tweets");
-------------------code----------------------

* Ensures **only one instance** of the screen exists
* If already on that screen → nothing happens

---

### 6.2 push()

-------------------code----------------------
navigation.push("Tweets");
-------------------code----------------------

* Always creates a **new instance**
* Stack grows (you can go back multiple times)

---

### Key Difference:

| Method   | Behavior                        |
| -------- | ------------------------------- |
| navigate | Reuses existing screen          |
| push     | Always adds new screen instance |

---

# 7. Navigation in Child Components (Problem)

The `navigation` prop is **only available in screens**:

-------------------code----------------------
<Stack.Screen name="Tweets" component={Tweets} />
-------------------code----------------------

So:

* `Tweets` → has navigation prop
* Child components inside it → DO NOT

---

# 8. Step 2 – Use useNavigation Hook

To access navigation in non-screen components:

-------------------code----------------------
import { useNavigation } from "@react-navigation/native";
-------------------code----------------------

---

# 9. Step 3 – Create Custom Link Component

-------------------code----------------------
import React from "react";
import { Button } from "react-native";
import { useNavigation } from "@react-navigation/native";

const Link = () => {
  const navigation = useNavigation();

  return (
    <Button
      title="Click"
      onPress={() => navigation.navigate("TweetDetails")}
    />
  );
};
-------------------code----------------------

### Explanation:

* `useNavigation()` gives access to navigation object
* Works inside any component

---

# 10. Step 4 – Use Link in Tweets Screen

-------------------code----------------------
const Tweets = () => {
  return (
    <Screen>
      <Link />
    </Screen>
  );
};
-------------------code----------------------

---

# 11. Full Final Code

-------------------code----------------------
import React from "react";
import { Text, Button } from "react-native";
import { NavigationContainer, useNavigation } from "@react-navigation/native";
import { createStackNavigator } from "@react-navigation/stack";

import Screen from "./app/components/Screen";

const Stack = createStackNavigator();

const Link = () => {
  const navigation = useNavigation();

  return (
    <Button
      title="Click"
      onPress={() => navigation.navigate("TweetDetails")}
    />
  );
};

const Tweets = () => (
  <Screen>
    <Link />
  </Screen>
);

const TweetDetails = () => (
  <Screen>
    <Text>Tweet Details</Text>
  </Screen>
);

const StackNavigator = () => (
  <Stack.Navigator>
    <Stack.Screen name="Tweets" component={Tweets} />
    <Stack.Screen name="TweetDetails" component={TweetDetails} />
  </Stack.Navigator>
);

const App = () => (
  <NavigationContainer>
    <StackNavigator />
  </NavigationContainer>
);

export default App;
-------------------code----------------------

---

# 12. Important Notes & Best Practices

* Use `navigate()` for normal navigation
* Use `push()` only when you need multiple instances
* Never pass navigation deeply through props → use `useNavigation`
* Keep navigation logic inside screens or hooks
* Name routes clearly and consistently

---

# 13. Summary

* Navigation between screens is done using `navigation.navigate()`
* Screen components receive `navigation` automatically
* Child components must use `useNavigation()`
* `navigate` vs `push` is a critical distinction:

  * `navigate` avoids duplicates
  * `push` creates new instances

Flow learned:

1. Add button
2. Use navigation prop
3. Navigate to screen
4. Use hook for child components
5. Understand stack behavior

This is the foundation for handling navigation interactions in real apps.

*** 5- Passing Parameters to Routes ***
# 1. Overview of the Lecture

This lecture explains how to **pass data (parameters) between screens** in React Navigation.

Key ideas:

* Use `navigation.navigate()` with parameters
* Access data using the `route` prop
* Understand how data flows between screens

---

# 2. Why Passing Parameters is Important

When navigating, you often need to send data such as:

* Item ID (e.g., tweet ID)
* Full object (e.g., tweet data)

Example:

* Tap a tweet → open its details → show correct data

---

# 3. Step 1 – Pass Parameters with navigate()

The `navigate()` function accepts a second argument (an object).

### Example:

-------------------code----------------------
navigation.navigate("TweetDetails", { id: 1 });
-------------------code----------------------

### Explanation:

* First argument → screen name
* Second argument → parameters object
* You can pass:

  * Single value (`id`)
  * Multiple values (`id`, `title`, etc.)
  * Entire object

---

# 4. Step 2 – Add Button with Parameters

-------------------code----------------------
const Tweets = ({ navigation }) => (
  <Screen>
    <Button
      title="View Tweet"
      onPress={() =>
        navigation.navigate("TweetDetails", { id: 1 })
      }
    />
  </Screen>
);
-------------------code----------------------

---

# 5. Step 3 – Access Parameters in Target Screen

React Navigation provides a **route prop** to screen components.

### Example:

-------------------code----------------------
const TweetDetails = ({ route }) => {
  return (
    <Screen>
      <Text>{route.params.id}</Text>
    </Screen>
  );
};
-------------------code----------------------

### Explanation:

* `route.params` contains the object passed from previous screen
* Access values using keys (`id`, `title`, etc.)

---

# 6. How Data Flow Works

1. User taps button
2. `navigate("TweetDetails", { id: 1 })` is called
3. React Navigation passes `{ id: 1 }`
4. Target screen receives it via `route.params`

---

# 7. Step 4 – Access Params in Child Components (Advanced)

Problem:

* `route` prop is only available in **screen components**

Solution:

* Use `useRoute()` hook

-------------------code----------------------
import { useRoute } from "@react-navigation/native";

const CustomComponent = () => {
  const route = useRoute();

  return <Text>{route.params.id}</Text>;
};
-------------------code----------------------

---

# 8. Full Final Code

-------------------code----------------------
import React from "react";
import { Text, Button } from "react-native";
import {
  NavigationContainer,
} from "@react-navigation/native";
import { createStackNavigator } from "@react-navigation/stack";

import Screen from "./app/components/Screen";

const Stack = createStackNavigator();

const Tweets = ({ navigation }) => (
  <Screen>
    <Button
      title="View Tweet"
      onPress={() =>
        navigation.navigate("TweetDetails", { id: 1 })
      }
    />
  </Screen>
);

const TweetDetails = ({ route }) => (
  <Screen>
    <Text>Tweet ID: {route.params.id}</Text>
  </Screen>
);

const StackNavigator = () => (
  <Stack.Navigator>
    <Stack.Screen name="Tweets" component={Tweets} />
    <Stack.Screen name="TweetDetails" component={TweetDetails} />
  </Stack.Navigator>
);

const App = () => (
  <NavigationContainer>
    <StackNavigator />
  </NavigationContainer>
);

export default App;
-------------------code----------------------

---

# 9. Important Notes & Best Practices

* Always pass parameters as an **object**
* Use clear names (`id`, `item`, `user`, etc.)
* Avoid passing large objects unless necessary
* Validate params before using them:

-------------------code----------------------
route?.params?.id
-------------------code----------------------

* Use `useRoute()` only when needed (child components)

---

# 10. Summary

* `navigate()` can send data using a second argument
* Data is received via `route.params`
* Screen components automatically get `route`
* Child components use `useRoute()` hook

Flow learned:

1. Add button
2. Pass params using navigate
3. Receive params via route
4. Use params in UI

This is essential for building real apps where screens depend on dynamic data.

*** 6- Setting Screen Titles ***
# 1. Overview of the Lecture

This lecture explains how to **customize screen titles** in React Navigation.

Key ideas:

* Default title comes from route name
* Use `options` prop to customize title
* Set **static titles**
* Set **dynamic titles based on route parameters**

---

# 2. Default Screen Title

By default:

-------------------code----------------------
<Stack.Screen name="TweetDetails" component={TweetDetails} />
-------------------code----------------------

* The header title = `"TweetDetails"`

---

# 3. Step 1 – Set Static Title

Use the `options` prop on `Stack.Screen`.

### Example:

-------------------code----------------------
<Stack.Screen
  name="TweetDetails"
  component={TweetDetails}
  options={{ title: "Tweet Details" }}
/>
-------------------code----------------------

### Result:

* Title becomes: **Tweet Details** (instead of TweetDetails)

---

# 4. Step 2 – Set Dynamic Title

Sometimes you want the title based on data (e.g., tweet ID).

### Use a function instead of an object:

-------------------code----------------------
<Stack.Screen
  name="TweetDetails"
  component={TweetDetails}
  options={({ route }) => ({
    title: "Tweet " + route.params.id,
  })}
/>
-------------------code----------------------

---

# 5. Important Syntax Detail

When using arrow function returning object:

-------------------code----------------------
options={({ route }) => ({
  title: "Tweet " + route.params.id,
})}
-------------------code----------------------

### Why parentheses?

* `{}` alone → treated as code block
* `({})` → treated as returned object

---

# 6. How Dynamic Title Works

1. Navigate with params:

-------------------code----------------------
navigation.navigate("TweetDetails", { id: 1 });
-------------------code----------------------

2. Access inside options:

-------------------code----------------------
route.params.id
-------------------code----------------------

3. Title updates dynamically:

* Example → `Tweet 1`

---

# 7. Step 3 – Complete Example with Navigation

### Tweets Screen

-------------------code----------------------
const Tweets = ({ navigation }) => (
  <Screen>
    <Button
      title="View Tweet"
      onPress={() =>
        navigation.navigate("TweetDetails", { id: 1 })
      }
    />
  </Screen>
);
-------------------code----------------------

---

### TweetDetails Screen

-------------------code----------------------
const TweetDetails = ({ route }) => (
  <Screen>
    <Text>Tweet ID: {route.params.id}</Text>
  </Screen>
);
-------------------code----------------------

---

# 8. Full Final Code

-------------------code----------------------
import React from "react";
import { Text, Button } from "react-native";
import { NavigationContainer } from "@react-navigation/native";
import { createStackNavigator } from "@react-navigation/stack";

import Screen from "./app/components/Screen";

const Stack = createStackNavigator();

const Tweets = ({ navigation }) => (
  <Screen>
    <Button
      title="View Tweet"
      onPress={() =>
        navigation.navigate("TweetDetails", { id: 1 })
      }
    />
  </Screen>
);

const TweetDetails = ({ route }) => (
  <Screen>
    <Text>Tweet ID: {route.params.id}</Text>
  </Screen>
);

const StackNavigator = () => (
  <Stack.Navigator>
    <Stack.Screen name="Tweets" component={Tweets} />
    <Stack.Screen
      name="TweetDetails"
      component={TweetDetails}
      options={({ route }) => ({
        title: "Tweet " + route.params.id,
      })}
    />
  </Stack.Navigator>
);

const App = () => (
  <NavigationContainer>
    <StackNavigator />
  </NavigationContainer>
);

export default App;
-------------------code----------------------

---

# 9. Important Notes & Best Practices

* Use `options` for all header customization
* Prefer dynamic titles when screen depends on data
* Always wrap returned object in `()` when using arrow function
* Ensure params exist before using them:

-------------------code----------------------
route?.params?.id
-------------------code----------------------

* Keep titles short and user-friendly

---

# 10. Summary

* Default title = route name
* Use `options` to override title
* Static title → simple object
* Dynamic title → function using `route`

Flow learned:

1. Define screen
2. Add `options`
3. Use static or dynamic title
4. Access route params for dynamic behavior

This is essential for making your app feel **dynamic and user-friendly**.

*** 7- Customizing Headers ***
# 1. Overview of the Lecture

This lecture explains how to **customize the header (top bar)** in React Navigation.

Key ideas:

* Customize header per screen using `options`
* Apply global styles using `screenOptions`
* Override global styles for specific screens

---

# 2. Header Customization Basics

Each screen (`Stack.Screen`) supports an `options` prop.

### Example:

-------------------code----------------------
<Stack.Screen
  name="Tweets"
  component={Tweets}
  options={{ title: "Tweets" }}
/>
-------------------code----------------------

---

# 3. Step 1 – Customize Header for One Screen

### Add background color

-------------------code----------------------
options={{
  headerStyle: { backgroundColor: "tomato" },
}}
-------------------code----------------------

---

### Change text color

-------------------code----------------------
options={{
  headerStyle: { backgroundColor: "tomato" },
  headerTintColor: "white",
}}
-------------------code----------------------

---

### Hide header

-------------------code----------------------
options={{
  headerShown: false,
}}
-------------------code----------------------

---

# 4. Important Behavior

* These styles apply **only to that screen**
* Other screens remain unchanged

---

# 5. Step 2 – Apply Global Header Styles

Instead of repeating styles for every screen, use `screenOptions` in the navigator.

-------------------code----------------------
<Stack.Navigator
  screenOptions={{
    headerStyle: { backgroundColor: "dodgerblue" },
    headerTintColor: "white",
  }}
>
-------------------code----------------------

---

# 6. How Global Styles Work

* Applied to **all screens by default**
* Cleaner and more maintainable

---

# 7. Step 3 – Override Global Styles

You can still override styles for specific screens:

-------------------code----------------------
<Stack.Screen
  name="Tweets"
  component={Tweets}
  options={{ headerShown: false }}
/>
-------------------code----------------------

### Result:

* Global style → blue header
* Tweets screen → no header

---

# 8. Common Header Options

| Property          | Description        |
| ----------------- | ------------------ |
| `headerStyle`     | Background styling |
| `headerTintColor` | Text & icon color  |
| `headerShown`     | Show/hide header   |
| `title`           | Header title       |

---

# 9. Step 4 – Complete Example

### Tweets Screen

-------------------code----------------------
const Tweets = ({ navigation }) => (
  <Screen>
    <Button
      title="View Tweet"
      onPress={() =>
        navigation.navigate("TweetDetails", { id: 1 })
      }
    />
  </Screen>
);
-------------------code----------------------

---

### TweetDetails Screen

-------------------code----------------------
const TweetDetails = ({ route }) => (
  <Screen>
    <Text>Tweet ID: {route.params.id}</Text>
  </Screen>
);
-------------------code----------------------

---

# 10. Full Final Code

-------------------code----------------------
import React from "react";
import { Text, Button } from "react-native";
import { NavigationContainer } from "@react-navigation/native";
import { createStackNavigator } from "@react-navigation/stack";

import Screen from "./app/components/Screen";

const Stack = createStackNavigator();

const Tweets = ({ navigation }) => (
  <Screen>
    <Button
      title="View Tweet"
      onPress={() =>
        navigation.navigate("TweetDetails", { id: 1 })
      }
    />
  </Screen>
);

const TweetDetails = ({ route }) => (
  <Screen>
    <Text>Tweet ID: {route.params.id}</Text>
  </Screen>
);

const StackNavigator = () => (
  <Stack.Navigator
    screenOptions={{
      headerStyle: { backgroundColor: "dodgerblue" },
      headerTintColor: "white",
    }}
  >
    <Stack.Screen
      name="Tweets"
      component={Tweets}
      options={{ headerShown: false }}
    />
    <Stack.Screen
      name="TweetDetails"
      component={TweetDetails}
    />
  </Stack.Navigator>
);

const App = () => (
  <NavigationContainer>
    <StackNavigator />
  </NavigationContainer>
);

export default App;
-------------------code----------------------

---

# 11. Important Notes & Best Practices

* Use `screenOptions` for global styles
* Override only when needed (avoid duplication)
* Keep header consistent across app
* Hide header only when necessary (e.g., full-screen UI)
* Use readable colors for accessibility

---

# 12. Summary

* `options` → customize header per screen
* `screenOptions` → apply global styles
* You can override global styles anytime

Flow learned:

1. Customize one screen
2. Apply global styles
3. Override specific screens

This gives you full control over your app’s header design.
