*** 1- Introduction ***
# Accessing Native Device Features in React Native

## 1. Introduction

This lecture introduces a new section focused on **accessing native device features in React Native applications**.

Mobile devices provide access to powerful hardware and system capabilities such as cameras, location services, storage, and sensors. React Native allows developers to interact with these features to build more advanced and practical mobile applications.

In this section, the application built earlier in the course will be **enhanced with new capabilities that rely on device features**.

---

# 2. Objective of This Section

The main goal of this section is to **extend the functionality of the existing app** by integrating native device capabilities.

Previously, the app likely handled basic UI and data operations. Now it will be upgraded to support features commonly found in real-world applications.

These improvements will allow the app to:

* Capture or select images
* Attach images to listings
* Retrieve the user's geographic location
* Include location data when posting listings

This represents a significant step toward building **production-ready mobile apps**.

---

# 3. Adding Images to Listings

One of the key improvements introduced in this section is the ability to **add images to listings**.

Users will be able to:

* Select images from their device
* Attach those images to a listing
* Submit the listing along with the selected images

Images are an essential feature in many applications such as:

* Marketplace apps
* Real estate apps
* Social platforms
* Product listing apps

Adding image support greatly improves the **usability and realism of the application**.

---

# 4. Including User Location

Another feature added in this section is **capturing the user's location**.

When a user posts a listing:

* The app will retrieve the device's geographic location
* The location data will be attached to the listing
* This information can later be used for location-based features

Examples of location-based features include:

* Showing nearby listings
* Displaying listings on a map
* Filtering listings by location

Location integration is commonly used in apps such as:

* Marketplace platforms
* Food delivery services
* Ride-sharing applications
* Local services apps

---

# 5. Moving the App to the Next Level

This section marks an important transition in the course.

The app will evolve from a **simple demonstration project** into a **more realistic mobile application** by incorporating device-level functionality.

Key improvements include:

* Media integration (images)
* Device data access (location)
* Enhanced listing submission

These capabilities reflect the types of features commonly required in **real production applications**.

---

# 6. Learning Goals for This Section

By completing this section, students will learn how to:

* Access native device capabilities in React Native
* Work with images inside a mobile application
* Capture and use user location data
* Attach device-generated data to app content

These skills are essential for building **fully functional mobile apps**.

---

# 7. Key Takeaways

* This section focuses on **accessing native device features**.
* The application will be enhanced to support **image uploads for listings**.
* User **location data will be captured and attached to listings**.
* These improvements transform the app into a **more realistic and feature-rich mobile application**.
* Learning to use device features is an essential skill for professional React Native development.

*** 2- Device Features ***
# Accessing Native Device Features in React Native — Overview

## 1. Introduction

This lecture provides an **overview of how React Native applications access native device features**.

Modern mobile apps often rely on device capabilities such as:

* Camera
* Contacts
* Location
* Calendar
* Media library

However, these features are **not fully included in the core React Native framework**. Instead, developers typically rely on **third-party libraries or tools like Expo** to access them.

---

# 2. Native Features in Core React Native

React Native includes a **small set of built-in APIs** under its Core APIs section.

These APIs provide limited access to device functionality.

Examples include:

* Displaying alerts
* Handling the soft keyboard
* Basic platform interactions

Example of a simple alert:

-------------------code----------------------
import { Alert } from "react-native";

Alert.alert("Hello", "This is an alert message");
-------------------code----------------------

Although useful, these APIs **do not provide access to most device hardware features**.

For more advanced capabilities, additional libraries are required.

---

# 3. Third-Party Libraries for Native Features

Most device features such as:

* Camera
* Contacts
* Location services
* Image selection

are provided through **third-party React Native libraries**.

One common example is the **Image Picker library**.

This library allows developers to:

* Select images from the device gallery
* Capture photos using the device camera

Example library:

-------------------code----------------------
react-native-image-picker
-------------------code----------------------

These libraries extend React Native by providing **access to native platform functionality**.

---

# 4. Additional Setup for Native Libraries

Many third-party libraries require **extra configuration in native projects**.

Typical installation steps include:

1. Installing the library using npm or yarn

Example:

-------------------code----------------------
npm install library-name
-------------------code----------------------

or

-------------------code----------------------
yarn add library-name
-------------------code----------------------

2. Running platform-specific setup

For example, iOS projects may require commands such as:

-------------------code----------------------
cd ios
pod install
-------------------code----------------------

Because these steps involve modifying **native Android or iOS code**, they are not always compatible with certain development environments.

---

# 5. Limitations When Using Expo

When using **Expo**, developers typically **do not have direct access to native iOS or Android project files**.

Because of this limitation:

* Some third-party libraries cannot be used directly in Expo.
* Libraries requiring native setup will not work unless additional steps are taken.

---

# 6. Options When Using Expo

Developers using Expo have two main options.

## Option 1: Eject from Expo

Ejecting converts an Expo project into a **full React Native project**.

Benefits:

* Full access to native Android and iOS code
* Ability to install any React Native library

Drawbacks:

* More complex setup
* Loss of some Expo conveniences

---

## Option 2: Use Expo APIs

Expo provides its own **collection of APIs that wrap native device functionality**.

These APIs are designed to work **without requiring native configuration**.

Advantages include:

* Simple installation
* No manual native setup
* Excellent documentation
* Faster development workflow

Because of these benefits, the instructor prefers continuing with **Expo APIs whenever possible**.

---

# 7. Advantages of Using Expo

Expo offers several benefits when working with device features.

### Faster Development

Expo reduces the amount of setup required for accessing native features.

### Better Productivity

Developers can focus on building features instead of configuring native code.

### Strong Documentation

Expo APIs are well documented and easy to understand.

### Continuous Improvement

Expo is actively developed and frequently adds new features.

---

# 8. Native Features Available in Expo

Expo provides many APIs that allow access to device capabilities.

Examples include:

* Camera
* Image picker
* Contacts
* Calendar
* Location
* Media library
* Sensors

These APIs are available through the **Expo documentation API reference**.

Each feature has:

* Detailed documentation
* Installation instructions
* Example usage

---

# 9. Focus of This Section

In this section of the course, the instructor will demonstrate how to use:

### Image Picker

The Image Picker allows users to:

* Select photos from the device gallery
* Capture images using the camera
* Attach images to app content

Other native features follow **similar usage patterns**, so once the Image Picker is understood, it becomes easier to learn other APIs independently.

---

# 10. Recommended Learning Step

Before continuing with the next lesson, students are encouraged to:

1. Visit the Expo documentation.
2. Review the list of available APIs.
3. Familiarize themselves with the device features that can be integrated into apps.

Because Expo continues to evolve, the list of supported features **continues to grow over time**.

---

# 11. Key Takeaways

* React Native core provides **limited access to native device features**.
* Most advanced features are implemented through **third-party libraries**.
* Many of these libraries require **native project configuration**.
* Expo simplifies access to native features through **built-in APIs**.
* Developers can either **eject from Expo** or **use Expo's built-in components**.
* This course will demonstrate how to use **Expo's Image Picker API** as an example of working with device features.

*** 3- Using ImagePicker ***
# Using the Expo Image Picker — Documentation Overview

## 1. Introduction

This lecture introduces the **Expo Image Picker component** and explains how to understand its documentation and install it in a React Native project.

The Image Picker allows applications to **access images or videos from the user's device**, either by selecting files from the device library or by capturing media using the camera.

---

# 2. What the Image Picker Can Do

The Expo Image Picker provides functionality to:

* Select images from the device photo library
* Select videos from the device library
* Capture images using the device camera
* Capture videos using the device camera

This functionality is commonly used in applications that require:

* User profile photos
* Product listings
* Social media uploads
* Media sharing features

---

# 3. Platform Compatibility Table

Each Expo API includes a **platform compatibility table** in its documentation.

This table shows which platforms support the component.

Typical platforms include:

* iOS
* Android
* Web

Before using any Expo component, developers should always check this table to ensure the component works on the desired platform.

Important note:

Some components **only work on real devices** and cannot be used in simulators or emulators.

Examples of such components include:

* Camera
* Sensors
* Certain hardware-based features

Therefore, when testing device-related features, it may be necessary to **run the application on a physical device**.

---

# 4. Installing the Image Picker

Installing Expo components is very simple.

The documentation usually provides a single command that installs the required package.

Example installation command:

-------------------code----------------------
expo install expo-image-picker
-------------------code----------------------

Running this command installs the correct version of the library compatible with the current Expo project.

One advantage of Expo is that **no additional setup is required after installation**.

---

# 5. Difference Between Expo and Bare React Native

The documentation also includes instructions for **bare React Native projects** (projects that do not use Expo).

In those projects, additional setup steps are required.

If a developer later decides to **eject from Expo**, these steps must be performed manually.

---

# 6. Native Configuration in Bare React Native

When using the Image Picker in a bare React Native project, additional configuration is required for each platform.

## 6.1 iOS Configuration

For iOS, developers must modify a configuration file called:

-------------------code----------------------
Info.plist
-------------------code----------------------

The name **plist** stands for **Property List**.

In this file, developers must add permission descriptions explaining why the application needs access to user data.

Typical examples include:

* Access to the camera
* Access to the photo library

After modifying the configuration, developers must run:

-------------------code----------------------
npx pod-install
-------------------code----------------------

This command installs the required native dependencies.

---

## 6.2 Android Configuration

Android also requires configuration changes in the native project.

These changes typically involve:

* Adding permissions
* Updating configuration files

Although the setup is manageable, it adds complexity compared to Expo's simplified approach.

---

# 7. Documentation Quality in Expo

One major advantage of Expo is the **quality of its documentation**.

Expo documentation typically includes:

* Installation instructions
* Working code examples
* Full API references
* Method descriptions
* Parameter explanations
* Return value details

This makes it easier for developers to quickly understand and implement features.

---

# 8. Comparison with Third-Party Libraries

Many third-party libraries outside the Expo ecosystem may have issues such as:

* Poor documentation
* Lack of maintenance
* Limited examples
* Unresolved bugs

When a library is poorly maintained and contains bugs, developers often have to **debug the problem themselves**.

Expo reduces these issues because:

* It is actively maintained
* Its APIs are consistently documented
* Its components are regularly updated

---

# 9. Installing the Image Picker in the Project

To add the Image Picker to the project, run the following command in the terminal:

-------------------code----------------------
expo install expo-image-picker
-------------------code----------------------

Once installed, the application can import and use the component to access media on the device.

---

# 10. Device Permissions

Accessing sensitive device data requires **explicit user permission**.

Examples of sensitive information include:

* Location data
* Photos and videos
* Camera access
* Contacts

Before the application can use these features, it must request permission from the user.

If the user denies permission, the application must handle this situation gracefully.

The next lesson focuses on **how to request and manage user permissions** when accessing device features.

---

# 11. Key Takeaways

* The Expo Image Picker allows apps to access images and videos from the device.
* The documentation includes a platform compatibility table to verify supported platforms.
* Installation is simple when using Expo and requires only one command.
* Bare React Native projects require additional native configuration.
* Expo provides well-documented APIs with examples and detailed references.
* Applications must request **user permission** before accessing sensitive device information such as photos or location.

*** 4- Requesting Permissions ***
# Requesting User Permission in React Native (Expo Image Picker)

## 1. Introduction

In this lecture, the goal is to **request permission from the user to access device features**, specifically the photo library using Expo Image Picker.

Instead of integrating this directly into the full app, the instructor temporarily **focuses on the feature in isolation** to understand how permissions work.

---

# 2. Starting Point

## 2.1 Basic App Structure

The current app simply renders a `Screen` component.

### Code

-------------------code----------------------
import Screen from "./app/components/Screen";

export default function App() {
  return <Screen></Screen>;
}
-------------------code----------------------

At this stage:

* No permission logic is implemented yet
* The app only renders the UI

---

# 3. Using useEffect Instead of componentDidMount

## 3.1 Why useEffect

In class components, we use:

-------------------code----------------------
componentDidMount()
-------------------code----------------------

In function components, we use:

-------------------code----------------------
useEffect()
-------------------code----------------------

a core concept in React, and once you understand it, a lot of things will become clearer.

---

# 1. What is a Component in React?

A **component** is simply a piece of UI.

Example:

* A button
* A screen
* A form

In React, there are **two main types of components**:

1. Class Components (old way)
2. Function Components (modern way)

---

# 2. Class Components (Old Way)

## 2.1 What is a Class Component?

A class component is written using JavaScript classes.

### Example

-------------------code----------------------
import React, { Component } from "react";
import { Text } from "react-native";

class MyComponent extends Component {
  render() {
    return <Text>Hello World</Text>;
  }
}

export default MyComponent;
-------------------code----------------------

---

## 2.2 Lifecycle Methods

Class components have something called **lifecycle methods**.

These are special methods that run at different times.

One of the most important ones:

### componentDidMount

-------------------code----------------------
componentDidMount() {
  console.log("Component mounted");
}
-------------------code----------------------

---

## 2.3 What does componentDidMount mean?

It runs:

* **After the component appears on the screen for the first time**

Think of it like:

"When the screen loads, run this code once"

---

## 2.4 Real Example

-------------------code----------------------
componentDidMount() {
  fetchData();
}
-------------------code----------------------

Use cases:

* API calls
* Asking permissions
* Initial setup

---

# 3. Function Components (Modern Way)

Today, React uses **function components** instead.

### Example

-------------------code----------------------
function MyComponent() {
  return <Text>Hello World</Text>;
}
-------------------code----------------------

Simpler and cleaner.

---

# 4. Problem: No Lifecycle Methods in Functions

Function components **do NOT have**:

* componentDidMount
* componentDidUpdate
* componentWillUnmount

So how do we do the same things?

---

# 5. Solution: useEffect Hook

React introduced something called **Hooks**.

The most important one here is:

### useEffect

---

# 6. Basic useEffect

-------------------code----------------------
import { useEffect } from "react";

useEffect(() => {
  console.log("This runs after render");
});
-------------------code----------------------

But this runs **every time the component renders**.

---

# 7. Making useEffect behave like componentDidMount

## 7.1 The Trick: Dependency Array

-------------------code----------------------
useEffect(() => {
  console.log("Runs only once");
}, []);
-------------------code----------------------

---

## 7.2 Why does this work?

The second argument:

-------------------code----------------------
[]
-------------------code----------------------

is called the **dependency array**.

Meaning:

* "Run this only when dependencies change"

But:

* There are no dependencies

So:

* It runs **only once**

---

# 8. Side-by-Side Comparison

## Class Component

-------------------code----------------------
componentDidMount() {
  console.log("Runs once");
}
-------------------code----------------------

## Function Component

-------------------code----------------------
useEffect(() => {
  console.log("Runs once");
}, []);
-------------------code----------------------

They do the **same thing**.

---

# 9. Simple Mental Model

Think like this:

* Class → componentDidMount → run once
* Function → useEffect + [] → run once

---

# 10. Why We Use This for Permissions

In your app, you wrote:

-------------------code----------------------
useEffect(() => {
  requestPermission();
}, []);
-------------------code----------------------

This means:

* When the screen loads for the first time
* Ask the user for permission
* Do not ask again on every render

---

# 11. What Happens Without [] (Important)

If you write:

-------------------code----------------------
useEffect(() => {
  requestPermission();
});
-------------------code----------------------

This will:

* Run every time the component re-renders
* Keep asking permission again and again

This is wrong behavior.

---

# 12. Summary

## Class Component

* Uses classes
* Has lifecycle methods
* componentDidMount runs once after first render

## Function Component

* Uses functions
* Uses hooks instead of lifecycle methods
* useEffect replaces lifecycle methods

## Key Rule

-------------------code----------------------
useEffect(() => {
  // run once
}, []);
-------------------code----------------------

= same as:

-------------------code----------------------
componentDidMount()
-------------------code----------------------

---


---

# 1. What Are Hooks (Very Simple)

Think of **hooks** like:

> Tools that give superpowers to function components

Before hooks:

* Function components were “dumb” (just UI)
* Only class components had logic (state, lifecycle)

After hooks:

* Function components can do everything

---

## Simple Definition

Hooks let you:

* Store data (state)
* Run code at the right time (like on load)

---

# 2. Most Important Hooks

You only need to understand these two for now:

1. `useState` → store and change data
2. `useEffect` → run code (like on start)

---

# 3. What is useState (Core Idea)

## Very Simple Idea

`useState` = a variable that can change and update the screen

---

## Normal JavaScript Variable (Problem)

-------------------code----------------------
let count = 0;

count = 1;
-------------------code----------------------

Problem:

* React does NOT update the UI

---

## useState Solution

-------------------code----------------------
import { useState } from "react";

const [count, setCount] = useState(0);
-------------------code----------------------

Now:

* `count` = value
* `setCount` = function to change it

---

# 4. Real Example (Super Simple)

-------------------code----------------------
import { useState } from "react";
import { Button, Text, View } from "react-native";

export default function App() {
  const [count, setCount] = useState(0);

  return (
    <View>
      <Text>{count}</Text>

      <Button
        title="Increase"
        onPress={() => setCount(count + 1)}
      />
    </View>
  );
}
-------------------code----------------------

---

## What Happens Here

1. App starts → count = 0
2. You press button
3. `setCount(1)` runs
4. React updates screen
5. Now shows 1

---

# 5. Easy Analogy

Think of `useState` like:

* `count` = current value
* `setCount` = remote control to change it

---

# 6. Rule You Must Remember

Never do this:

-------------------code----------------------
count = 5;
-------------------code----------------------

Always do this:

-------------------code----------------------
setCount(5);
-------------------code----------------------

---

# 7. What is useEffect (Simple Again)

## Very Simple Idea

`useEffect` = run code at a specific time

---

## Example: Run once when app starts

-------------------code----------------------
import { useEffect } from "react";

useEffect(() => {
  console.log("App started");
}, []);
-------------------code----------------------

---

## In Your Case (Permission)

-------------------code----------------------
useEffect(() => {
  requestPermission();
}, []);
-------------------code----------------------

Meaning:

* When screen opens → ask permission

---

# 8. Simple Comparison

| Thing     | What it does                   |
| --------- | ------------------------------ |
| useState  | stores and updates data        |
| useEffect | runs code (on start or change) |

---

# 9. Real Life Analogy

Think of your app like a game:

## useState

* Player health
* Score
* Level

These change → screen updates

## useEffect

* Start game
* Load data
* Ask permission

These run at specific times

---

# 10. How They Work Together

Example:

-------------------code----------------------
const [name, setName] = useState("");

useEffect(() => {
  console.log("Component loaded");
}, []);
-------------------code----------------------

* `useState` → stores data
* `useEffect` → runs logic

---

# 11. Why Hooks Are Important

Without hooks:

* You must use class components (more complex)

With hooks:

* Code is shorter
* Easier to read
* More modern

---

# 12. Super Short Summary

* Hooks = tools for function components
* `useState` = store and update data
* `useEffect` = run code at the right time
* `[]` in useEffect = run only once

---

---

## 3.2 Basic useEffect Behavior

-------------------code----------------------
useEffect(() => {
  // code here runs on every render
});
-------------------code----------------------

Problem:

* This runs **every time the component re-renders**
* We do NOT want to request permission repeatedly

---

## 3.3 Run Only Once (Important Concept)

To run the effect only once:

-------------------code----------------------
useEffect(() => {
  // runs only once
}, []);
-------------------code----------------------

Explanation:

* The second parameter is a **dependency array**
* An empty array means:

  * No dependencies
  * Runs only on first render
* Equivalent to `componentDidMount`

---

# 4. Importing Expo Image Picker

To access permission methods:

-------------------code----------------------
import * as ImagePicker from "expo-image-picker";
-------------------code----------------------

This gives access to multiple methods including:

* Requesting permissions
* Opening camera
* Accessing media library

---

# 5. Requesting Permission

## 5.1 The Permission Method

We use:

-------------------code----------------------
ImagePicker.requestCameraRollPermissionsAsync()
-------------------code----------------------

This is an **async function** that returns a result object.

---

## 5.2 Problem with Async in useEffect

Incorrect approach:

-------------------code----------------------
useEffect(async () => {
  const result = await ImagePicker.requestCameraRollPermissionsAsync();
}, []);
-------------------code----------------------

Problem:

* `useEffect` cannot accept an async function
* Async functions return a Promise
* `useEffect` expects a cleanup function, not a Promise

---

# 6. Correct Solution: Separate Async Function

## 6.1 Step 1 — Create Async Function

-------------------code----------------------
const requestPermission = async () => {
  const result = await ImagePicker.requestCameraRollPermissionsAsync();
};
-------------------code----------------------

---

## 6.2 Step 2 — Call Function Inside useEffect

-------------------code----------------------
useEffect(() => {
  requestPermission();
}, []);
-------------------code----------------------

Important:

* Do NOT use `await` inside `useEffect`
* Do NOT make `useEffect` async

---

# 7. Handling the Permission Response

## 7.1 Response Object

The result contains properties such as:

* `granted` (boolean)
* `status`
* `canAskAgain`
* `expires`

---

## 7.2 Using Object Destructuring

Instead of:

-------------------code----------------------
const result = await ImagePicker.requestCameraRollPermissionsAsync();

if (!result.granted) {
  alert("Permission required");
}
-------------------code----------------------

Use:

-------------------code----------------------
const { granted } = await ImagePicker.requestCameraRollPermissionsAsync();

if (!granted) {
  alert("You need to enable permission to access the library.");
}
-------------------code----------------------

Cleaner and more readable.

---

# 8. Full Step-by-Step Implementation

## Step 1 — Import Dependencies

-------------------code----------------------
import { useEffect } from "react";
import * as ImagePicker from "expo-image-picker";
import Screen from "./app/components/Screen";
-------------------code----------------------

---

## Step 2 — Create Permission Function

-------------------code----------------------
const requestPermission = async () => {
  const { granted } = await ImagePicker.requestCameraRollPermissionsAsync();

  if (!granted) {
    alert("You need to enable permission to access the library.");
  }
};
-------------------code----------------------

---

## Step 3 — Call It Once Using useEffect

-------------------code----------------------
useEffect(() => {
  requestPermission();
}, []);
-------------------code----------------------

---

## Step 4 — Render App

-------------------code----------------------
return <Screen />;
-------------------code----------------------

---

# 9. Full Final Code

-------------------code----------------------
import { useEffect } from "react";
import * as ImagePicker from "expo-image-picker";
import Screen from "./app/components/Screen";

export default function App() {
  const requestPermission = async () => {
    const { granted } = await ImagePicker.requestCameraRollPermissionsAsync();

    if (!granted) {
      alert("You need to enable permission to access the library.");
    }
  };

  useEffect(() => {
    requestPermission();
  }, []);

  return <Screen />;
}
-------------------code----------------------

---

# 10. How Permission Behavior Works

## First Run

* App asks for permission
* User sees system dialog
* User chooses Allow or Deny

## After First Decision

* Device remembers the choice
* No dialog appears again
* Permission result is returned immediately

---

## If Permission Is Denied

* App should show a message
* User must manually enable permission in device settings

Example (iPhone):

* Settings → App → Photos → Enable access

---

# 11. Important Notes and Best Practices

## 11.1 Never Make useEffect Async

Incorrect:

-------------------code----------------------
useEffect(async () => {})
-------------------code----------------------

Correct:

* Use a separate async function

---

## 11.2 Always Use Dependency Array

-------------------code----------------------
useEffect(() => {}, []);
-------------------code----------------------

Prevents repeated execution.

---

## 11.3 Handle Permission Denial Gracefully

* Always check `granted`
* Show clear message to the user
* Do not crash or block the app

---

## 11.4 Keep Logic Clean

* Use destructuring for cleaner code
* Separate logic into functions

---

## 12. Summary

* Permissions are required to access sensitive device features
* `useEffect` is used to run code when the component mounts
* An empty dependency array ensures the effect runs only once
* Async functions cannot be used directly inside `useEffect`
* The correct approach is to define an async function and call it inside `useEffect`
* The permission result includes a `granted` boolean
* If permission is denied, the app should notify the user
* Once permission is decided, the system remembers it and does not prompt again

This pattern is essential and will be reused for other features such as:

* Camera access
* Location services
* File system access

*** 5- Permissions Module ***
# Using the Permissions API in React Native (Expo)

## 1. Introduction

In this lecture, we learn another way to **request user permissions** using the **Permissions API** from Expo.

Previously, we used:

-------------------code----------------------
ImagePicker.requestCameraRollPermissionsAsync()
-------------------code----------------------

Now we explore a more general approach using:

-------------------code----------------------
expo-permissions
-------------------code----------------------

---

# 2. Installing the Permissions API

## Step 1 — Install Package

Run the following command:

-------------------code----------------------bash
expo install expo-permissions
-------------------code----------------------

This package provides **centralized control for multiple permissions**.

---

# 3. Importing the Permissions Module

## Step 2 — Import in Your Code

-------------------code----------------------
import * as Permissions from "expo-permissions";
-------------------code----------------------

This gives access to different permission types like:

* Camera
* Camera Roll (Photos)
* Location
* Audio
* Calendar

---

# 4. Requesting Permission with Permissions API

## Step 3 — Create Permission Function

-------------------code----------------------
const requestPermission = async () => {
  const { granted } = await Permissions.askAsync(Permissions.CAMERA_ROLL);

  if (!granted) {
    alert("You need to enable permission to access the library.");
  }
};
-------------------code----------------------

---

## Explanation

* `Permissions.askAsync()` requests permission
* You pass the permission type:

-------------------code----------------------
Permissions.CAMERA_ROLL
-------------------code----------------------

* It returns an object with:

-------------------code----------------------
{ granted: true/false }
-------------------code----------------------

---

# 5. Using Multiple Permissions

One advantage of the Permissions API:

## Step 4 — Request Multiple Permissions

-------------------code----------------------
const requestPermission = async () => {
  const { granted } = await Permissions.askAsync(
    Permissions.CAMERA_ROLL,
    Permissions.LOCATION
  );

  if (!granted) {
    alert("You need to enable permissions.");
  }
};
-------------------code----------------------

---

## Behavior

* User will see **multiple permission dialogs**
* If the user denies **any one of them**:

  * `granted` becomes `false`

---

# 6. Connecting with useEffect

## Step 5 — Run on App Start

-------------------code----------------------
useEffect(() => {
  requestPermission();
}, []);
-------------------code----------------------

This ensures:

* Permission is requested **only once**
* Runs when app loads

---

# 7. Comparison with ImagePicker Method

## ImagePicker Method

-------------------code----------------------
ImagePicker.requestCameraRollPermissionsAsync();
-------------------code----------------------

## Permissions API Method

-------------------code----------------------
Permissions.askAsync(Permissions.CAMERA_ROLL);
-------------------code----------------------

---

## Key Insight

The lecture explains:

* The ImagePicker method **internally uses the Permissions API**
* Both approaches are **functionally similar**

---

# 8. Which Approach Should You Use?

## Permissions API

Use when:

* You need **multiple permissions at once**

## ImagePicker Method

Use when:

* You only need **one specific permission**
* Simpler and cleaner

The instructor chooses:

* **ImagePicker request method for simplicity**

---

# 9. Full Step-by-Step Code (Permissions API Version)

## Step 1 — Imports

-------------------code----------------------
import { useEffect } from "react";
import * as Permissions from "expo-permissions";
import Screen from "./app/components/Screen";
-------------------code----------------------

---

## Step 2 — Permission Function

-------------------code----------------------
const requestPermission = async () => {
  const { granted } = await Permissions.askAsync(Permissions.CAMERA_ROLL);

  if (!granted) {
    alert("You need to enable permission to access the library.");
  }
};
-------------------code----------------------

---

## Step 3 — useEffect

-------------------code----------------------
useEffect(() => {
  requestPermission();
}, []);
-------------------code----------------------

---

## Step 4 — Render

-------------------code----------------------
return <Screen />;
-------------------code----------------------

---

# 10. Full Final Code

-------------------code----------------------
import { useEffect } from "react";
import * as Permissions from "expo-permissions";
import Screen from "./app/components/Screen";

export default function App() {
  const requestPermission = async () => {
    const { granted } = await Permissions.askAsync(Permissions.CAMERA_ROLL);

    if (!granted) {
      alert("You need to enable permission to access the library.");
    }
  };

  useEffect(() => {
    requestPermission();
  }, []);

  return <Screen />;
}
-------------------code----------------------

---

# 11. Important Notes and Best Practices

## 11.1 Permissions Are Required

Always request permission before accessing:

* Photos
* Camera
* Location
* Contacts

---

## 11.2 Handle Denied Permissions

Always check:

-------------------code----------------------
if (!granted)
-------------------code----------------------

Never assume permission is granted.

---

## 11.3 useEffect Must Not Be Async

Incorrect:

-------------------code----------------------
useEffect(async () => {})
-------------------code----------------------

Correct:

* Use a separate async function

---

## 11.4 Use Empty Dependency Array

-------------------code----------------------
useEffect(() => {}, []);
-------------------code----------------------

Ensures:

* Runs only once
* Prevents repeated permission requests

---

## 11.5 Choose the Right Approach

* Use **Permissions API** for flexibility
* Use **ImagePicker method** for simplicity

---

# 12. Summary

* The Permissions API allows requesting **one or multiple permissions**
* It is installed using `expo-permissions`
* Use `Permissions.askAsync()` to request access
* It returns a result object with a `granted` boolean
* If any permission is denied, `granted` becomes false
* This approach is similar to ImagePicker’s request method
* In practice, simpler methods are often preferred unless multiple permissions are needed

This completes the permission handling part.
Next step is learning how to **select images from the device library**.

*** 6- Accessing the Library ***
# Selecting an Image from the Device Library in React Native (Expo)

## 1. Introduction

In this lecture, we learn how to:

* Open the device image library
* Let the user select an image
* Display the selected image on the screen

This is done using the **Expo Image Picker API** along with React hooks.

---

# 2. Adding a Button to Trigger Image Selection

## Step 1 — Add a Button

We start by adding a button that triggers the image selection.

-------------------code----------------------
import { Button } from "react-native";

<Button title="Select Image" onPress={selectImage} />
-------------------code----------------------

---

## Explanation

* `title` → text shown on the button
* `onPress` → function that runs when user taps the button

---

# 3. Creating the selectImage Function

## Step 2 — Define the Function

-------------------code----------------------
const selectImage = async () => {
  
};
-------------------code----------------------

This function must be **async** because we will call an async API.

---

# 4. Opening the Image Library

## Step 3 — Use Image Picker

-------------------code----------------------
import * as ImagePicker from "expo-image-picker";

const selectImage = async () => {
  const result = await ImagePicker.launchImageLibraryAsync();
};
-------------------code----------------------

---

## Explanation

* `launchImageLibraryAsync()` opens the device gallery
* Returns a result object after user action

---

# 5. Handling Errors with try/catch

## Step 4 — Add Error Handling

-------------------code----------------------
const selectImage = async () => {
  try {
    const result = await ImagePicker.launchImageLibraryAsync();
  } catch (error) {
    console.log("Error reading an image", error);
  }
};
-------------------code----------------------

---

## Why this is important

* Prevents app crashes
* Handles unexpected failures safely

---

# 6. Understanding the Result Object

The result contains useful properties:

### 6.1 canceled

-------------------code----------------------
result.canceled
-------------------code----------------------

* `true` → user canceled selection
* `false` → user selected an image

---

### 6.2 uri

-------------------code----------------------
result.uri
-------------------code----------------------

* Path to the selected image
* Used to display the image

---

# 7. Using useState to Store Image

## Step 5 — Create State Variable

-------------------code----------------------
import { useState } from "react";

const [imageUri, setImageUri] = useState(null);
-------------------code----------------------

---

## Explanation

* `imageUri` → stores image path
* `setImageUri` → updates the value

---

# 8. Saving Selected Image

## Step 6 — Update State

-------------------code----------------------
const selectImage = async () => {
  try {
    const result = await ImagePicker.launchImageLibraryAsync();

    if (!result.canceled) {
      setImageUri(result.uri);
    }
  } catch (error) {
    console.log("Error reading an image", error);
  }
};
-------------------code----------------------

---

## Explanation

* Only update state if user selects an image
* Prevents errors when user cancels

---

# 9. Displaying the Image

## Step 7 — Render Image Component

-------------------code----------------------
import { Image } from "react-native";

<Image source={{ uri: imageUri }} style={{ width: 200, height: 200 }} />
-------------------code----------------------

---

## Explanation

* `source={{ uri: imageUri }}` → loads image from path
* `style` → controls image size

---

# 10. Complete Step-by-Step Implementation

## Step 1 — Imports

-------------------code----------------------
import { useState } from "react";
import { View, Button, Image } from "react-native";
import * as ImagePicker from "expo-image-picker";
-------------------code----------------------

---

## Step 2 — State

-------------------code----------------------
const [imageUri, setImageUri] = useState(null);
-------------------code----------------------

---

## Step 3 — Function

-------------------code----------------------
const selectImage = async () => {
  try {
    const result = await ImagePicker.launchImageLibraryAsync();

    if (!result.canceled) {
      setImageUri(result.uri);
    }
  } catch (error) {
    console.log("Error reading an image", error);
  }
};
-------------------code----------------------

---

## Step 4 — UI

-------------------code----------------------
<View>
  <Button title="Select Image" onPress={selectImage} />
  <Image source={{ uri: imageUri }} style={{ width: 200, height: 200 }} />
</View>
-------------------code----------------------

---

# 11. Full Final Code

-------------------code----------------------
import * as ImagePicker from "expo-image-picker";
import { useState } from "react";
import { Button, Image } from "react-native";

export default function App() {
  const [imageUri, setImageUri] = useState(null);

  const selectImage = async () => {
    try {
      const result = await ImagePicker.launchImageLibraryAsync();

      if (!result.canceled) {
        setImageUri(result.uri);
      }
    } catch (error) {
      console.log("Error reading an image", error);
    }
  };

  return (
    <Screen>
      <Button title="Select Image" onPress={selectImage} />
      {imageUri && (
        <Image source={{ uri: imageUri }} style={{ width: 200, height: 200 }} />
      )}
    </Screen>
  );
}
-------------------code----------------------

---

# 12. Important Notes and Best Practices

## 12.1 Always Check canceled

-------------------code----------------------
if (!result.canceled)
-------------------code----------------------

Prevents errors when user cancels.

---

## 12.2 Use try/catch

Always wrap async calls to:

* Avoid crashes
* Handle unexpected issues

---

## 12.3 Use Conditional Rendering

-------------------code----------------------
{imageUri && <Image ... />}
-------------------code----------------------

Prevents rendering empty images.

---

## 12.4 Keep UI Simple for Testing

During development:

* Use fixed width/height
* Focus on functionality first

---

## 12.5 Permissions Must Be Granted First

Make sure:

* Permission logic is implemented before using image picker

---

# 13. Summary

* A button is used to trigger image selection
* `launchImageLibraryAsync()` opens the gallery
* The result contains `canceled` and `uri`
* `useState` stores the selected image path
* The `Image` component displays the selected image
* Error handling is done using try/catch
* Expo makes working with native features simple and fast

This completes the flow of selecting and displaying an image from the device.


*** 7- Exercises ***
# Building Image Selection Feature — Exercise Overview and Strategy

## 1. Introduction

This lecture explains the **feature you will build as an exercise** and provides a **step-by-step strategy** to approach it effectively.

The goal is not just to build the feature, but to **learn how to break complex problems into smaller parts**.

---

# 2. Final Feature Overview

You will build an image selection system with the following functionality:

## 2.1 Add Images

* User can select and add images

## 2.2 Delete Images

* Tapping an existing image removes it

## 2.3 Send Images to Server

* When submitting the form:

  * Image URIs are included in the request

## 2.4 Validation

* If no images are selected:

  * Show error message:

-------------------code----------------------
Please select at least one image
-------------------code----------------------

---

# 3. Key Development Principle

## 3.1 Break Down the Problem

Instead of building everything at once:

* Divide the feature into **small, manageable parts**
* Build and test each part independently

This approach:

* Reduces complexity
* Makes debugging easier
* Improves code quality

---

# 4. Step-by-Step Implementation Plan

## 4.1 Step 1 — Ignore the Main Form

Do not start with the listing form.

Instead:

* Pretend it does not exist
* Focus only on the image feature

---

## 4.2 Step 2 — Build ImageInput Component

### Goal

Create a reusable component called:

-------------------code----------------------
ImageInput
-------------------code----------------------

### Features

* Select a single image
* Display the selected image
* Delete the image when tapped

---

### Example Structure

-------------------code----------------------
const ImageInput = () => {
  const [imageUri, setImageUri] = useState(null);

  const selectImage = async () => {
    // open image picker
  };

  const handlePress = () => {
    // delete image
  };

  return (
    // show image or placeholder
  );
};
-------------------code----------------------

---

### Behavior

| Action         | Result        |
| -------------- | ------------- |
| Tap empty area | Select image  |
| Image selected | Display image |
| Tap image      | Delete image  |

---

## 4.3 Step 3 — Test ImageInput in Isolation

Use it directly inside `App.js`:

-------------------code----------------------
export default function App() {
  return <ImageInput />;
}
-------------------code----------------------

Focus only on:

* Single image selection
* Display
* Deletion

Do not add multiple images yet.

---

## 4.4 Step 4 — Build ImageInputList Component

### Goal

Create a component that supports **multiple images**:

-------------------code----------------------
ImageInputList
-------------------code----------------------

---

### Responsibilities

* Manage a list of image URIs
* Allow adding multiple images
* Display all selected images

---

### Example Structure

-------------------code----------------------
const ImageInputList = () => {
  const [imageUris, setImageUris] = useState([]);

  const handleAdd = (uri) => {
    setImageUris([...imageUris, uri]);
  };

  const handleRemove = (uri) => {
    setImageUris(imageUris.filter((u) => u !== uri));
  };

  return (
    // render list of ImageInput components
  );
};
-------------------code----------------------

---

## 4.5 Step 5 — Test ImageInputList in Isolation

Again, use it in `App.js`:

-------------------code----------------------
export default function App() {
  return <ImageInputList />;
}
-------------------code----------------------

Ensure:

* Multiple images can be added
* Images can be removed individually

---

## 4.6 Step 6 — Integrate with Form

After both components work correctly:

* Add `ImageInputList` to the listing form
* Connect it to form data

---

## 4.7 Step 7 — Add Validation

Validate that at least one image is selected:

-------------------code----------------------
if (imageUris.length === 0) {
  alert("Please select at least one image");
}
-------------------code----------------------

---

# 5. Development Strategy

## 5.1 Build in Isolation

Always:

* Build components separately
* Test them independently

---

## 5.2 Use Incremental Steps

Do not jump to the final solution.

Follow this order:

1. Single image
2. Multiple images
3. Form integration
4. Validation

---

## 5.3 Focus on One Problem at a Time

Example:

* First: Can I select an image?
* Then: Can I display it?
* Then: Can I delete it?

---

# 6. Important Notes and Best Practices

## 6.1 Reusable Components

Design components like:

* `ImageInput` (single image)
* `ImageInputList` (multiple images)

Reusable components:

* Improve code structure
* Make maintenance easier

---

## 6.2 Avoid Complexity Early

Do not:

* Mix form logic with image logic
* Build everything at once

---

## 6.3 Think in Layers

* UI Layer → ImageInput
* Logic Layer → ImageInputList
* Form Layer → Validation and submission

---

## 6.4 Practice Through Exercises

The instructor emphasizes:

* Exercises are essential
* Learning happens by doing

---

# 7. Summary

* You will build an image selection system with add, delete, and validation features
* The feature should be developed step-by-step, not all at once
* Start with a single image component (`ImageInput`)
* Then build a multi-image component (`ImageInputList`)
* Test each component independently before integration
* Finally, connect everything to the form and add validation
* Breaking problems into smaller steps is a key development skill

This approach helps you build complex features in a **clear, structured, and maintainable way**.

*** 8- Building ImageInput- Layout ***
# Building the ImageInput Component in React Native

## 1. Introduction

In this lecture, we start building a reusable component called:

-------------------code----------------------
ImageInput
-------------------code----------------------

This component will:

* Display a placeholder (camera icon) when no image is selected
* Display an image when one is provided
* Act as a base for future features (add/delete images)

---

# 2. Creating the Component

## Step 1 — Create File

Create a new file:

-------------------code----------------------
app/components/ImageInput.js
-------------------code----------------------

---

## Step 2 — Generate Component Template

Using the snippet:

-------------------code----------------------
rnfc
-------------------code----------------------

This generates a basic React Native function component.

---

## Step 3 — Basic Component Structure

-------------------code----------------------
function ImageInput(props) {
  return (
    <View style={styles.container}></View>
  );
}
-------------------code----------------------

---

# 3. Styling the Container

## Step 4 — Add Styles

-------------------code----------------------
const styles = StyleSheet.create({
  container: {
    backgroundColor: colors.light,
    borderRadius: 15,
    height: 100,
    width: 100,
    alignItems: "center",
    justifyContent: "center"
  }
});
-------------------code----------------------

---

## Explanation

* `backgroundColor` → light background
* `borderRadius` → rounded corners
* `height/width` → fixed size
* `alignItems` + `justifyContent` → center content

---

# 4. Rendering the Component

## Step 5 — Use in App.js

-------------------code----------------------
import ImageInput from "./app/components/ImageInput";

export default function App() {
  return <ImageInput />;
}
-------------------code----------------------

Now you should see a styled square container.

---

# 5. Showing Camera Icon (No Image)

## Step 6 — Import Icon Library

-------------------code----------------------
import { MaterialCommunityIcons } from "@expo/vector-icons";
-------------------code----------------------

---

## Step 7 — Add imageUri Prop

-------------------code----------------------
function ImageInput({ imageUri }) {
-------------------code----------------------

---

## Step 8 — Conditional Rendering (No Image)

-------------------code----------------------
{!imageUri && (
  <MaterialCommunityIcons
    name="camera"
    size={40}
    color={colors.medium}
  />
)}
-------------------code----------------------

---

## Explanation

* If no image → show camera icon
* `!imageUri` means "image does not exist"

---

# 6. Displaying Selected Image

## Step 9 — Import Image Component

-------------------code----------------------
import { Image } from "react-native";
-------------------code----------------------

---

## Step 10 — Conditional Rendering (Image Exists)

-------------------code----------------------
{imageUri && (
  <Image source={{ uri: imageUri }} style={styles.image} />
)}
-------------------code----------------------

---

## Step 11 — Add Image Styles

-------------------code----------------------
image: {
  width: "100%",
  height: "100%"
}
-------------------code----------------------

---

## Explanation

* Image fills entire container
* Uses URI from state

---

# 7. Testing with Real Image

## Step 12 — Pass imageUri from App

-------------------code----------------------
export default function App() {
  const [imageUri, setImageUri] = useState(null);

  return <ImageInput imageUri={imageUri} />;
}
-------------------code----------------------

After selecting an image, it should appear inside the container.

---

# 8. Fixing Border Radius Issue

## Problem

* Image appears but **rounded corners disappear**

## Reason

* Image is slightly larger than container
* It overflows outside rounded edges

---

## Step 13 — Fix with overflow

-------------------code----------------------
container: {
  backgroundColor: colors.light,
  borderRadius: 15,
  height: 100,
  width: 100,
  alignItems: "center",
  justifyContent: "center",
  overflow: "hidden"
}
-------------------code----------------------

---

## Explanation

* `overflow: "hidden"` clips extra image parts
* Keeps rounded corners visible

---

# 9. Full Step-by-Step Implementation

## Step 1 — Imports

-------------------code----------------------
import React from "react";
import { View, StyleSheet, Image } from "react-native";
import { MaterialCommunityIcons } from "@expo/vector-icons";
import colors from "../config/colors";
-------------------code----------------------

---

## Step 2 — Component

-------------------code----------------------
function ImageInput({ imageUri }) {
  return (
    <View style={styles.container}>
      {!imageUri && (
        <MaterialCommunityIcons
          name="camera"
          size={40}
          color={colors.medium}
        />
      )}

      {imageUri && (
        <Image source={{ uri: imageUri }} style={styles.image} />
      )}
    </View>
  );
}
-------------------code----------------------

---

## Step 3 — Styles

-------------------code----------------------
const styles = StyleSheet.create({
  container: {
    backgroundColor: colors.light,
    borderRadius: 15,
    height: 100,
    width: 100,
    alignItems: "center",
    justifyContent: "center",
    overflow: "hidden"
  },
  image: {
    width: "100%",
    height: "100%"
  }
});
-------------------code----------------------

---

# 10. Full Final Code

-------------------code----------------------
import React from "react";
import { View, StyleSheet, Image } from "react-native";
import { MaterialCommunityIcons } from "@expo/vector-icons";
import colors from "../config/colors";

function ImageInput({ imageUri }) {
  return (
    <View style={styles.container}>
      {!imageUri && (
        <MaterialCommunityIcons
          name="camera"
          size={40}
          color={colors.medium}
        />
      )}

      {imageUri && (
        <Image source={{ uri: imageUri }} style={styles.image} />
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    backgroundColor: colors.light,
    borderRadius: 15,
    height: 100,
    width: 100,
    alignItems: "center",
    justifyContent: "center",
    overflow: "hidden"
  },
  image: {
    width: "100%",
    height: "100%"
  }
});

export default ImageInput;
-------------------code----------------------

---

# 11. Important Notes and Best Practices

## 11.1 Use Conditional Rendering

-------------------code----------------------
!imageUri → show icon  
imageUri → show image
-------------------code----------------------

---

## 11.2 Always Handle Overflow

-------------------code----------------------
overflow: "hidden"
-------------------code----------------------

Important when using:

* borderRadius
* images

---

## 11.3 Keep Components Reusable

* `ImageInput` should not handle logic yet
* Only display UI
* Logic will be added later

---

## 11.4 Clean Style Order

Organize styles like:

-------------------code----------------------
alignItems
justifyContent
-------------------code----------------------

Improves readability.

---

# 12. Summary

* Created a reusable `ImageInput` component
* Styled a container with rounded corners
* Displayed a camera icon when no image exists
* Displayed an image when `imageUri` is provided
* Fixed border radius issue using `overflow: hidden`
* Used conditional rendering to switch between states

This component is the **foundation** for building:

* Image selection
* Image deletion
* Multiple image lists

*** 9- Building ImageInput- Touches ***
# Handling Touches and Making ImageInput Fully Reusable

## 1. Introduction

In this lecture, we enhance the `ImageInput` component by:

* Handling user taps
* Opening the image library
* Deleting an image on tap
* Making the component reusable
* Moving permission logic inside the component

This transforms `ImageInput` into a **fully interactive and reusable component**.

---

# 2. Handling User Touch

## Step 1 — Wrap with Touchable Component

We wrap the container with:

-------------------code----------------------
import { TouchableWithoutFeedback } from "react-native";
-------------------code----------------------

---

## Step 2 — Add onPress

-------------------code----------------------
<TouchableWithoutFeedback onPress={handlePress}>
  <View style={styles.container}>
    ...
  </View>
</TouchableWithoutFeedback>
-------------------code----------------------

---

## Important Note

Always import touchables from:

-------------------code----------------------
react-native
-------------------code----------------------

Not from gesture handler libraries unless needed.

---

# 3. Creating handlePress Function

## Step 3 — Basic Logic

-------------------code----------------------
const handlePress = () => {
  if (!imageUri) {
    selectImage();
  } else {
    // show delete alert
  }
};
-------------------code----------------------

---

## Explanation

* No image → open library
* Has image → ask to delete

---

# 4. Selecting an Image Inside Component

## Step 4 — Import ImagePicker

-------------------code----------------------
import * as ImagePicker from "expo-image-picker";
-------------------code----------------------

---

## Step 5 — selectImage Function

-------------------code----------------------
const selectImage = async () => {
  try {
    const result = await ImagePicker.launchImageLibraryAsync({
      mediaTypes: ImagePicker.MediaTypeOptions.Images,
      quality: 0.5,
    });

    if (!result.canceled) {
      onChangeImage(result.uri);
    }
  } catch (error) {
    console.log("Error reading an image", error);
  }
};
-------------------code----------------------

---

## Explanation

### mediaTypes

-------------------code----------------------
ImagePicker.MediaTypeOptions.Images
-------------------code----------------------

* Allows only images (not videos)

---

### quality

-------------------code----------------------
quality: 0.5
-------------------code----------------------

* Reduces image size
* Improves upload performance

---

# 5. Lifting State Up (Important Concept)

## Problem

Previously:

* Component had its own state
* Also received `imageUri` as prop

This creates duplication.

---

## Solution

Use **props instead of local state**

### Add Prop

-------------------code----------------------
function ImageInput({ imageUri, onChangeImage })
-------------------code----------------------

---

## Update Image

-------------------code----------------------
onChangeImage(result.uri);
-------------------code----------------------

---

## Explanation

* Parent controls state
* Component only notifies changes

---

# 6. Handling Image Deletion

## Step 6 — Import Alert

-------------------code----------------------
import { Alert } from "react-native";
-------------------code----------------------

---

## Step 7 — Show Confirmation Dialog

-------------------code----------------------
Alert.alert(
  "Delete",
  "Are you sure you want to delete this image?",
  [
    {
      text: "Yes",
      onPress: () => onChangeImage(null),
    },
    {
      text: "No",
    },
  ]
);
-------------------code----------------------

---

## Explanation

* "Yes" → remove image (set null)
* "No" → do nothing

---

# 7. Updating handlePress

## Final Version

-------------------code----------------------
const handlePress = () => {
  if (!imageUri) {
    selectImage();
  } else {
    Alert.alert(
      "Delete",
      "Are you sure you want to delete this image?",
      [
        { text: "Yes", onPress: () => onChangeImage(null) },
        { text: "No" },
      ]
    );
  }
};
-------------------code----------------------

---

# 8. Moving Permission Logic Inside Component

## Step 8 — Add useEffect

-------------------code----------------------
import { useEffect } from "react";
-------------------code----------------------

---

## Step 9 — Request Permission

-------------------code----------------------
useEffect(() => {
  requestPermission();
}, []);
-------------------code----------------------

---

## Step 10 — Permission Function

-------------------code----------------------
const requestPermission = async () => {
  const { granted } = await ImagePicker.requestCameraRollPermissionsAsync();

  if (!granted) {
    alert("You need to enable permission to access the library.");
  }
};
-------------------code----------------------

---

## Why This Is Important

* Makes component **self-contained**
* No need to remember permissions in parent
* Improves reusability

---

# 9. Updating App.js

## Step 11 — Use ImageInput Properly

-------------------code----------------------
import { useState } from "react";
import Screen from "./app/components/Screen";
import ImageInput from "./app/components/ImageInput";

export default function App() {
  const [imageUri, setImageUri] = useState(null);

  return (
    <Screen>
      <ImageInput
        imageUri={imageUri}
        onChangeImage={(uri) => setImageUri(uri)}
      />
    </Screen>
  );
}
-------------------code----------------------

---

## Remove Old Code

Delete:

* Button
* Manual image display

---

# 10. Full Final Code

## ImageInput.js

-------------------code----------------------
import React, { useEffect } from "react";
import {
  View,
  StyleSheet,
  Image,
  TouchableWithoutFeedback,
  Alert,
} from "react-native";
import * as ImagePicker from "expo-image-picker";
import { MaterialCommunityIcons } from "@expo/vector-icons";
import colors from "../config/colors";

function ImageInput({ imageUri, onChangeImage }) {
  useEffect(() => {
    requestPermission();
  }, []);

  const requestPermission = async () => {
    const { granted } =
      await ImagePicker.requestCameraRollPermissionsAsync();

    if (!granted) {
      alert("You need to enable permission to access the library.");
    }
  };

  const selectImage = async () => {
    try {
      const result = await ImagePicker.launchImageLibraryAsync({
        mediaTypes: ImagePicker.MediaTypeOptions.Images,
        quality: 0.5,
      });

      if (!result.canceled) {
        onChangeImage(result.uri);
      }
    } catch (error) {
      console.log("Error reading an image", error);
    }
  };

  const handlePress = () => {
    if (!imageUri) {
      selectImage();
    } else {
      Alert.alert(
        "Delete",
        "Are you sure you want to delete this image?",
        [
          { text: "Yes", onPress: () => onChangeImage(null) },
          { text: "No" },
        ]
      );
    }
  };

  return (
    <TouchableWithoutFeedback onPress={handlePress}>
      <View style={styles.container}>
        {!imageUri && (
          <MaterialCommunityIcons
            name="camera"
            size={40}
            color={colors.medium}
          />
        )}

        {imageUri && (
          <Image source={{ uri: imageUri }} style={styles.image} />
        )}
      </View>
    </TouchableWithoutFeedback>
  );
}

const styles = StyleSheet.create({
  container: {
    backgroundColor: colors.light,
    borderRadius: 15,
    height: 100,
    width: 100,
    alignItems: "center",
    justifyContent: "center",
    overflow: "hidden",
  },
  image: {
    width: "100%",
    height: "100%",
  },
});

export default ImageInput;
-------------------code----------------------

---

## App.js

-------------------code----------------------
import { useState } from "react";
import Screen from "./app/components/Screen";
import ImageInput from "./app/components/ImageInput";

export default function App() {
  const [imageUri, setImageUri] = useState(null);

  return (
    <Screen>
      <ImageInput
        imageUri={imageUri}
        onChangeImage={(uri) => setImageUri(uri)}
      />
    </Screen>
  );
}
-------------------code----------------------

---

# 11. Important Notes and Best Practices

## 11.1 Lift State Up

* Component should not manage duplicate state
* Parent handles data

---

## 11.2 Use Props for Communication

-------------------code----------------------
onChangeImage(uri)
-------------------code----------------------

---

## 11.3 Keep Components Reusable

* Handle permissions inside component
* Do not depend on parent logic

---

## 11.4 Always Confirm Destructive Actions

* Use alert before deleting

---

## 11.5 Optimize Image Size

-------------------code----------------------
quality: 0.5
-------------------code----------------------

Important for:

* Performance
* Upload speed

---

# 12. Summary

* Added touch handling using `TouchableWithoutFeedback`
* Implemented image selection inside component
* Added delete confirmation using `Alert`
* Moved state management to parent component
* Used props (`imageUri`, `onChangeImage`) for communication
* Moved permission logic into component for reusability
* Built a fully reusable and interactive `ImageInput` component

This component is now ready to be used inside:

* Forms
* Lists
* Multi-image inputs

*** 10- Building ImageInputList- Basics ***
# Building ImageInputList (Multiple Image Selector)

## 1. Introduction

In this lecture, we build a new component:

-------------------code----------------------
ImageInputList
-------------------code----------------------

This component allows:

* Displaying multiple images
* Adding new images
* Removing existing images

It works together with the previously built `ImageInput` component.

---

# 2. Component Purpose and Design

## 2.1 Responsibilities

`ImageInputList` should:

* Receive a list of image URIs
* Render multiple `ImageInput` components
* Handle adding images
* Handle removing images

---

## 2.2 Important Design Decision

* This component **does NOT manage state**
* It only:

  * Receives data via props
  * Sends events back to parent

This keeps it **clean and reusable**

---

# 3. Creating the Component

## Step 1 — Create File

-------------------code----------------------
app/components/ImageInputList.js
-------------------code----------------------

---

## Step 2 — Basic Structure

-------------------code----------------------
function ImageInputList({ imageUris = [], onAddImage, onRemoveImage }) {
  return (
    <View style={styles.container}>
      {/* content here */}
    </View>
  );
}
-------------------code----------------------

---

# 4. Layout Images Horizontally

## Step 3 — Add Flexbox Styling

-------------------code----------------------
const styles = StyleSheet.create({
  container: {
    flexDirection: "row"
  }
});
-------------------code----------------------

---

## Explanation

* `flexDirection: "row"` → places images side by side

---

# 5. Rendering Existing Images

## Step 4 — Map Through imageUris

-------------------code----------------------
{imageUris.map((uri) => (
  <ImageInput
    key={uri}
    imageUri={uri}
    onChangeImage={() => onRemoveImage(uri)}
  />
))}
-------------------code----------------------

---

## Explanation

* `map()` creates one `ImageInput` per image
* `key` is required in React lists
* `onChangeImage` → used here to **delete image**

---

# 6. Adding New Images

## Step 5 — Add Empty ImageInput

-------------------code----------------------
<ImageInput
  onChangeImage={(uri) => onAddImage(uri)}
/>
-------------------code----------------------

---

## Explanation

* This input has **no imageUri**
* Acts as a button to add a new image
* When user selects an image → triggers `onAddImage`

---

# 7. Adding Spacing Between Images

## Step 6 — Wrap Each Item in View

-------------------code----------------------
{imageUris.map((uri) => (
  <View key={uri} style={styles.image}>
    <ImageInput
      imageUri={uri}
      onChangeImage={() => onRemoveImage(uri)}
    />
  </View>
))}
-------------------code----------------------

---

## Step 7 — Add Margin

-------------------code----------------------
image: {
  marginRight: 10
}
-------------------code----------------------

---

## Explanation

* Adds spacing between images
* Keeps UI clean

---

# 8. Updating App.js

## Step 8 — State Setup

-------------------code----------------------
const [imageUris, setImageUris] = useState([]);
-------------------code----------------------

---

## Step 9 — Add Image Handler

-------------------code----------------------
const handleAdd = (uri) => {
  setImageUris([...imageUris, uri]);
};
-------------------code----------------------

---

## Step 10 — Remove Image Handler

-------------------code----------------------
const handleRemove = (uri) => {
  setImageUris(imageUris.filter((imageUri) => imageUri !== uri));
};
-------------------code----------------------

---

## Explanation

### Adding

-------------------code----------------------
[...imageUris, uri]
-------------------code----------------------

* Copies existing array
* Adds new item

---

### Removing

-------------------code----------------------
filter((imageUri) => imageUri !== uri)
-------------------code----------------------

* Keeps all items except the one to remove

---

# 9. Using ImageInputList in App

## Step 11 — Render Component

-------------------code----------------------
<ImageInputList
  imageUris={imageUris}
  onAddImage={handleAdd}
  onRemoveImage={handleRemove}
/>
-------------------code----------------------

---

# 10. Full Final Code

## ImageInputList.js

-------------------code----------------------
import React from "react";
import { View, StyleSheet } from "react-native";
import ImageInput from "./ImageInput";

function ImageInputList({ imageUris = [], onAddImage, onRemoveImage }) {
  return (
    <View style={styles.container}>
      {imageUris.map((uri) => (
        <View key={uri} style={styles.image}>
          <ImageInput
            imageUri={uri}
            onChangeImage={() => onRemoveImage(uri)}
          />
        </View>
      ))}

      <ImageInput onChangeImage={(uri) => onAddImage(uri)} />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flexDirection: "row"
  },
  image: {
    marginRight: 10
  }
});

export default ImageInputList;
-------------------code----------------------

---

## App.js

-------------------code----------------------
import { useState } from "react";
import Screen from "./app/components/Screen";
import ImageInputList from "./app/components/ImageInputList";

export default function App() {
  const [imageUris, setImageUris] = useState([]);

  const handleAdd = (uri) => {
    setImageUris([...imageUris, uri]);
  };

  const handleRemove = (uri) => {
    setImageUris(imageUris.filter((imageUri) => imageUri !== uri));
  };

  return (
    <Screen>
      <ImageInputList
        imageUris={imageUris}
        onAddImage={handleAdd}
        onRemoveImage={handleRemove}
      />
    </Screen>
  );
}
-------------------code----------------------

---

# 11. Important Notes and Best Practices

## 11.1 Do Not Store State in Child

* `ImageInputList` does not use `useState`
* Parent (`App`) controls state

---

## 11.2 Use Props for Communication

* `onAddImage`
* `onRemoveImage`

---

## 11.3 Use Unique Keys

-------------------code----------------------
key={uri}
-------------------code----------------------

Required for list rendering

---

## 11.4 Keep Components Simple

* `ImageInput` → single image
* `ImageInputList` → list logic

---

## 11.5 Avoid Inline Functions When Possible

Instead of:

-------------------code----------------------
(uri) => handleAdd(uri)
-------------------code----------------------

Use:

-------------------code----------------------
handleAdd
-------------------code----------------------

Cleaner and simpler

---

# 12. Summary

* Built `ImageInputList` for handling multiple images
* Used `map()` to render multiple `ImageInput` components
* Added support for:

  * Adding images
  * Removing images
* Managed state in parent component (`App`)
* Used spread operator to add items
* Used filter method to remove items
* Improved UI with spacing between images

This completes the multi-image selection system and prepares you for integrating it into forms.


*** 11- Building ImageInputList- Scrolling ***
# Enhancing ImageInputList with Scrolling

## 1. Introduction

In this lecture, we improve the `ImageInputList` component by solving a UI problem:

* When many images are added, the **camera icon disappears**
* User can no longer add new images

### Solution

* Enable **horizontal scrolling**
* Automatically scroll to the end when a new image is added

---

# 2. Problem Explanation

## 2.1 Current Behavior

* Images are displayed in a row
* When the row overflows:

  * Extra images go off-screen
  * Add button (camera icon) becomes hidden

---

## 2.2 Goal

* Allow horizontal scrolling
* Keep "Add Image" accessible
* Improve user experience with auto-scroll

---

# 3. Adding Horizontal Scrolling

## Step 1 — Import ScrollView

-------------------code----------------------
import { ScrollView } from "react-native";
-------------------code----------------------

---

## Step 2 — Wrap Content

-------------------code----------------------
<ScrollView horizontal>
  {/* images */}
</ScrollView>
-------------------code----------------------

---

## Explanation

* `horizontal` → enables horizontal scrolling
* Default is vertical scrolling

---

# 4. Updated Layout Structure

## Step 3 — Wrap Inside View

-------------------code----------------------
<View>
  <ScrollView horizontal>
    {/* images */}
  </ScrollView>
</View>
-------------------code----------------------

---

## Why Wrap with View?

* `ScrollView` expands to fill available space
* Wrapping it in `View`:

  * Limits its size
  * Prevents layout issues in forms

---

# 5. Auto-Scroll to End

## 5.1 Problem

* When adding new image:

  * It appears off-screen
  * User must scroll manually

---

## 5.2 Solution

Use:

* `useRef` → get reference to ScrollView
* `scrollToEnd()` → scroll automatically

---

# 6. Using useRef

## Step 4 — Import Hook

-------------------code----------------------
import { useRef } from "react";
-------------------code----------------------

---

## Step 5 — Create Reference

-------------------code----------------------
const scrollView = useRef();
-------------------code----------------------

---

## Step 6 — Attach to ScrollView

-------------------code----------------------
<ScrollView ref={scrollView} horizontal>
-------------------code----------------------

---

## Explanation

* `scrollView.current` → gives access to ScrollView instance

---

# 7. Trigger Auto Scroll

## Step 7 — Use onContentSizeChange

-------------------code----------------------
onContentSizeChange={() => scrollView.current.scrollToEnd()}
-------------------code----------------------

---

## Full ScrollView Example

-------------------code----------------------
<ScrollView
  ref={scrollView}
  horizontal
  onContentSizeChange={() => scrollView.current.scrollToEnd()}
>
-------------------code----------------------

---

## Explanation

* Runs when content size changes
* Automatically scrolls to the end

---

# 8. Complete Updated Component

## ImageInputList.js

-------------------code----------------------
import React, { useRef } from "react";
import { View, StyleSheet, ScrollView } from "react-native";
import ImageInput from "./ImageInput";

function ImageInputList({ imageUris = [], onAddImage, onRemoveImage }) {
  const scrollView = useRef();

  return (
    <View>
      <ScrollView
        horizontal
        ref={scrollView}
        onContentSizeChange={() => scrollView.current.scrollToEnd()}
      >
        {imageUris.map((uri) => (
          <View key={uri} style={styles.image}>
            <ImageInput
              imageUri={uri}
              onChangeImage={() => onRemoveImage(uri)}
            />
          </View>
        ))}

        <ImageInput onChangeImage={(uri) => onAddImage(uri)} />
      </ScrollView>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flexDirection: "row"
  },
  image: {
    marginRight: 10
  }
});

export default ImageInputList;
-------------------code----------------------

---

# 9. Important Notes and Best Practices

## 9.1 Always Use ScrollView for Overflow

* Use `ScrollView` when content can exceed screen width

---

## 9.2 Use horizontal for Row Layouts

-------------------code----------------------
<ScrollView horizontal />
-------------------code----------------------

---

## 9.3 Use useRef for Component Methods

* Access instance methods like:

-------------------code----------------------
scrollView.current.scrollToEnd()
-------------------code----------------------

---

## 9.4 Wrap ScrollView in View

Avoid layout issues:

-------------------code----------------------
<View>
  <ScrollView />
</View>
-------------------code----------------------

---

## 9.5 Auto Scroll Improves UX

* Keeps latest content visible
* Removes need for manual scrolling

---

## 9.6 Ref Does Not Provide IntelliSense

* `useRef()` is generic
* No auto-suggestions for methods
* Still works correctly

---

# 10. Summary

* Identified UI issue: add button becomes hidden
* Solved using `ScrollView` with horizontal scrolling
* Used `useRef` to access ScrollView methods
* Implemented auto-scroll using `onContentSizeChange`
* Prevented layout issues by wrapping in a `View`
* Improved usability and user experience

The `ImageInputList` component is now fully functional and user-friendly, ready for integration into forms.


*** 12- Building FormImagePicker ***
# Integrating ImageInputList with Formik (FormImagePicker)

## 1. Introduction

In this lecture, we create a new component:

-------------------code----------------------
FormImagePicker
-------------------code----------------------

This component integrates:

* `ImageInputList` (UI for images)
* `Formik` (form state management)
* `ErrorMessage` (validation feedback)

This follows the same pattern used for:

* `AppFormField` (text input)
* `AppFormPicker` (dropdown)

---

# 2. Goal of FormImagePicker

## 2.1 Responsibilities

* Display multiple images using `ImageInputList`
* Connect image data to Formik state
* Handle adding and removing images
* Show validation errors

---

## 2.2 Key Concept

* No local state
* All data comes from **Formik context**

---

# 3. Creating the Component

## Step 1 — Create File

-------------------code----------------------
app/components/forms/FormImagePicker.js
-------------------code----------------------

---

## Step 2 — Basic Structure

-------------------code----------------------
import React from "react";

function FormImagePicker() {
  return null;
}

export default FormImagePicker;
-------------------code----------------------

---

# 4. Using Formik Context

## Step 3 — Import Hook

-------------------code----------------------
import { useFormikContext } from "formik";
-------------------code----------------------

---

## Step 4 — Access Form State

-------------------code----------------------
const { values, errors, touched, setFieldValue } = useFormikContext();
-------------------code----------------------

---

## Explanation

* `values` → form data
* `errors` → validation errors
* `touched` → tracks user interaction
* `setFieldValue` → update form state

---

# 5. Adding Props

## Step 5 — Add name Prop

-------------------code----------------------
function FormImagePicker({ name })
-------------------code----------------------

---

## Explanation

* `name` → identifies the field in Formik
* Example: `"images"`

---

# 6. Connecting ImageInputList

## Step 6 — Render Component

-------------------code----------------------
<ImageInputList imageUris={values[name]} />
-------------------code----------------------

---

## Explanation

* `values[name]` → array of image URIs

---

# 7. Handling Add Image

## Step 7 — Create handleAdd

-------------------code----------------------
const handleAdd = (uri) => {
  const imageUris = values[name];
  setFieldValue(name, [...imageUris, uri]);
};
-------------------code----------------------

---

## Explanation

* Copies existing images
* Adds new URI
* Updates Formik state

---

# 8. Handling Remove Image

## Step 8 — Create handleRemove

-------------------code----------------------
const handleRemove = (uri) => {
  const imageUris = values[name];
  setFieldValue(
    name,
    imageUris.filter((imageUri) => imageUri !== uri)
  );
};
-------------------code----------------------

---

## Explanation

* Removes selected image from array

---

# 9. Pass Handlers to Component

## Step 9 — Update ImageInputList

-------------------code----------------------
<ImageInputList
  imageUris={values[name]}
  onAddImage={handleAdd}
  onRemoveImage={handleRemove}
/>
-------------------code----------------------

---

# 10. Adding Error Message

## Step 10 — Import Component

-------------------code----------------------
import ErrorMessage from "./ErrorMessage";
-------------------code----------------------

---

## Step 11 — Render Error

-------------------code----------------------
<ErrorMessage error={errors[name]} visible={touched[name]} />
-------------------code----------------------

---

## Explanation

* Shows error only if field is touched

---

# 11. Using Fragment

## Step 12 — Wrap Components

-------------------code----------------------
<>
  <ImageInputList ... />
  <ErrorMessage ... />
</>
-------------------code----------------------

---

## Why Fragment?

* Allows returning multiple components
* Avoids unnecessary View wrapper

---

# 12. Code Refactoring (Optional Improvement)

## Step 13 — Create Alias

-------------------code----------------------
const imageUris = values[name];
-------------------code----------------------

---

## Replace Usage

-------------------code----------------------
imageUris
-------------------code----------------------

instead of:

-------------------code----------------------
values[name]
-------------------code----------------------

---

## Benefit

* Cleaner and more readable code

---

# 13. Full Final Code

## FormImagePicker.js

-------------------code----------------------
import React from "react";
import { useFormikContext } from "formik";

import ImageInputList from "../ImageInputList";
import ErrorMessage from "./ErrorMessage";

function FormImagePicker({ name }) {
  const { values, errors, touched, setFieldValue } = useFormikContext();

  const imageUris = values[name];

  const handleAdd = (uri) => {
    setFieldValue(name, [...imageUris, uri]);
  };

  const handleRemove = (uri) => {
    setFieldValue(
      name,
      imageUris.filter((imageUri) => imageUri !== uri)
    );
  };

  return (
    <>
      <ImageInputList
        imageUris={imageUris}
        onAddImage={handleAdd}
        onRemoveImage={handleRemove}
      />
      <ErrorMessage error={errors[name]} visible={touched[name]} />
    </>
  );
}

export default FormImagePicker;
-------------------code----------------------

---

# 14. Important Notes and Best Practices

## 14.1 Do Not Use Local State

* Formik manages all form data
* Avoid `useState` inside form components

---

## 14.2 Always Use setFieldValue

-------------------code----------------------
setFieldValue(name, value)
-------------------code----------------------

* Updates specific field in Formik

---

## 14.3 Keep Components Consistent

Follow same pattern as:

* `AppFormField`
* `AppFormPicker`

---

## 14.4 Use name Prop Carefully

* Must match Formik field name
* Example:

-------------------code----------------------
<FormImagePicker name="images" />
-------------------code----------------------

---

## 14.5 Avoid Code Duplication

* Use alias:

-------------------code----------------------
const imageUris = values[name];
-------------------code----------------------

---

# 15. Summary

* Created `FormImagePicker` to integrate images with Formik
* Used `useFormikContext` to access form state
* Passed image data via `values[name]`
* Used `setFieldValue` to update images
* Implemented add and remove logic
* Displayed validation errors using `ErrorMessage`
* Followed reusable and clean architecture

This component completes the integration of image selection into form handling and prepares it for use inside a full form such as a listing editor.
