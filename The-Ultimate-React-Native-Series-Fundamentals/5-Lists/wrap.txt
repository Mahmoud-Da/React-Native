*** 1- Introduction ***
---

# Building Lists in React Native

## 1. Introduction

Almost every mobile application needs to display a **list of data**.

Examples:

* Messages in a chat app
* Products in a marketplace
* Notifications
* Contacts
* Posts in a social feed

Because lists are so common, understanding how to build them properly is a core React Native skill.

---

## 2. Why Lists Are Important

Lists allow you to:

1. Display dynamic data from an array
2. Render large amounts of content efficiently
3. Build scalable and reusable UI patterns
4. Improve performance using optimized components like `FlatList`

In real-world apps, most screens are built around lists.

---

## 3. What You Will Build in This Section

In this section, you will:

1. Build three new screens
2. Create multiple reusable components
3. Learn how to structure list-based layouts
4. Improve your app architecture
5. Make your UI more dynamic and production-ready

This section moves the app from static layouts to data-driven interfaces.

---

## 4. Core Concept: Rendering Lists

In React Native, lists are usually built using:

* `FlatList` (recommended for most cases)
* `SectionList` (for grouped data)
* `ScrollView` (not recommended for large lists)

The most important one is:

### FlatList

It:

* Efficiently renders only visible items
* Handles large datasets well
* Supports separators, headers, pull-to-refresh, and more

---

## 5. High-Level Example of a Basic FlatList

Here is a simple conceptual example:

-------------------code----------------------
import React from "react";
import { View, Text, FlatList } from "react-native";

const data = [
  { id: "1", title: "First Item" },
  { id: "2", title: "Second Item" },
  { id: "3", title: "Third Item" },
];

function ExampleScreen() {
  return (
    <View>
      <FlatList
        data={data}
        keyExtractor={(item) => item.id}
        renderItem={({ item }) => (
          <Text>{item.title}</Text>
        )}
      />
    </View>
  );
}

export default ExampleScreen;
-------------------code----------------------

This is the foundation of list rendering in React Native.

---

## 6. What You Should Focus On

As you move into this section, focus on:

1. Understanding how `FlatList` works
2. Passing data to reusable components
3. Separating UI into small components
4. Avoiding performance issues
5. Writing clean and scalable list-based code

Lists are one of the most important patterns in mobile development.

---

## 7. Best Practices for Lists

* Always provide a stable `keyExtractor`
* Avoid inline heavy logic inside `renderItem`
* Use reusable components for list items
* Avoid using `ScrollView` for long lists
* Keep list items lightweight

---

## 8. Summary

* Lists are fundamental to almost every app
* React Native provides optimized tools like `FlatList`
* This section introduces new screens and reusable components
* You will move from static UI to dynamic data-driven interfaces
* Mastering lists is essential for building real-world mobile apps

This marks the beginning of building scalable, professional app structures using lists.


*** 2- FlatList ***

---

# Building the Messages Screen with FlatList

## 1. Goal of This Screen

The goal of this lesson is to build a **Messages Screen** that displays a list of messages using React Native’s `FlatList` component.

This is the first real example of rendering **dynamic lists of data** in the app.

---

## 2. Why Use FlatList

`FlatList` is the standard way to render lists in React Native because it is:

1. Fully cross-platform (iOS and Android)
2. Optimized for performance
3. Able to render large datasets efficiently
4. Supports advanced features such as:

   * Headers and footers
   * Item separators
   * Pull-to-refresh
   * Infinite scrolling

---

## 3. Creating the Messages Screen

### Step 1: Create the Screen File

Create a new file inside the `screens` folder:

-------------------code----------------------
MessagesScreen.js
-------------------code----------------------

Then create a function component.

-------------------code----------------------
import React from "react";
import { View, FlatList } from "react-native";

function MessagesScreen() {
  return (
    <View>
      <FlatList />
    </View>
  );
}

export default MessagesScreen;
-------------------code----------------------

---

## 4. Preparing Temporary Data

Since the app is not yet connected to a backend, we use **hard-coded data** as a temporary solution.

### Step 2: Create a Messages Array

Each message object contains:

* `id` (unique identifier)
* `title`
* `description`
* `image` (user avatar)

-------------------code----------------------
const messages = [
  {
    id: 1,
    title: "t1",
    description: "d1",
    image: require("../assets/mosh.jpg"),
  },
  {
    id: 2,
    title: "t2",
    description: "d2",
    image: require("../assets/mosh.jpg"),
  },
];
-------------------code----------------------

Important:

* `require()` is required for static assets
* The relative path goes one level up from `screens`

---

## 5. Configuring FlatList

`FlatList` requires **three essential props**.

---

### 5.1 data Prop

This tells FlatList what data to render.

-------------------code----------------------
data={messages}
-------------------code----------------------

---

### 5.2 keyExtractor Prop

Each item in a list must have a **unique key**.

FlatList requires keys to be **strings**, so we convert `id` to a string.

-------------------code----------------------
keyExtractor={(message) => message.id.toString()}
-------------------code----------------------

---

### 5.3 renderItem Prop

This function tells FlatList **how to render each item**.

The parameter is an object that includes:

* `item`
* `index`
* `separators`

We destructure `item` directly.

-------------------code----------------------
renderItem={({ item }) => (
  <ListItem
    title={item.title}
    subtitle={item.description}
    image={item.image}
  />
)}
-------------------code----------------------

---

## 6. Reusing the ListItem Component

Instead of building UI inline, we reuse the existing **ListItem** component.

### Step 3: Import ListItem

-------------------code----------------------
import ListItem from "../components/ListItem";
-------------------code----------------------

This keeps the code:

* Clean
* Reusable
* Easy to maintain

---

## 7. Testing the Screen

### Step 4: Render MessagesScreen in App.js

-------------------code----------------------
import MessagesScreen from "./screens/MessagesScreen";

export default function App() {
  return <MessagesScreen />;
}
-------------------code----------------------

After saving:

* The list renders correctly
* The UI is basic and not styled yet
* Improvements will come in later lessons

---

## 8. Known Issue: Safe Area

### Problem

The list is not inside a **safe area**, so:

* Content may be hidden behind the status bar
* Notches on iPhones may overlap content

### Solution

This will be fixed in the next lesson using a Safe Area wrapper.

---

## 9. Best Practices Highlighted

* Use `FlatList` instead of `ScrollView` for lists
* Always provide a stable `keyExtractor`
* Convert numeric IDs to strings
* Use reusable components (`ListItem`)
* Keep temporary mock data simple
* Avoid styling too early; focus on structure first

---

## 10. Full Final Code (MessagesScreen)

-------------------code----------------------
import React from "react";
import { View, FlatList } from "react-native";
import ListItem from "../components/ListItem";

const messages = [
  {
    id: 1,
    title: "t1",
    description: "d1",
    image: require("../assets/mosh.jpg"),
  },
  {
    id: 2,
    title: "t2",
    description: "d2",
    image: require("../assets/mosh.jpg"),
  },
];

function MessagesScreen() {
  return (
    <View>
      <FlatList
        data={messages}
        keyExtractor={(message) => message.id.toString()}
        renderItem={({ item }) => (
          <ListItem
            title={item.title}
            subtitle={item.description}
            image={item.image}
          />
        )}
      />
    </View>
  );
}

export default MessagesScreen;
-------------------code----------------------

---

## 11. Summary

* `FlatList` is the core tool for rendering lists in React Native
* MessagesScreen uses temporary mock data
* Three required FlatList props were introduced:

  1. `data`
  2. `keyExtractor`
  3. `renderItem`
* The `ListItem` component was reused for clean UI
* The safe area issue was identified and deferred to the next lesson

This screen forms the foundation for more advanced list features coming next.


*** 3- Extracting the Screen Component ***

---

# Fixing Safe Area Issues and Creating a Reusable Screen Component

## 1. Problem: Content Under the Status Bar

When rendering the `MessagesScreen`, the content appears:

* Under the status bar on Android
* Properly spaced on iOS (after using `SafeAreaView`)

### Why?

* `SafeAreaView` only fully solves this issue on **iOS**
* On Android, additional padding is required

---

# Part 1: Basic SafeAreaView Fix

## 2. Wrapping Content in SafeAreaView

### Step 1: Import SafeAreaView

-------------------code----------------------
import { SafeAreaView } from "react-native";
-------------------code----------------------

### Step 2: Wrap the Screen Content

-------------------code----------------------
<SafeAreaView>
  <FlatList ... />
</SafeAreaView>
-------------------code----------------------

Result:

* iOS issue resolved
* Android still has overlap

---

# Part 2: Fixing Android with Platform and StatusBar

## 3. Detect Platform and Apply Padding

### Step 1: Import Required APIs

-------------------code----------------------
import { StyleSheet, Platform, StatusBar } from "react-native";
-------------------code----------------------

### Step 2: Create a Style for the Screen

-------------------code----------------------
const styles = StyleSheet.create({
  screen: {
    paddingTop:
      Platform.OS === "android" ? StatusBar.currentHeight : 0,
  },
});
-------------------code----------------------

### Step 3: Apply the Style

-------------------code----------------------
<SafeAreaView style={styles.screen}>
  <FlatList ... />
</SafeAreaView>
-------------------code----------------------

Result:

* Works on iOS
* Works on Android

However, this approach requires repeating the same logic in every screen.

---

# Part 3: Better Approach Using expo-constants

Instead of manually checking the platform, we can use `expo-constants`.

## 4. Install expo-constants

-------------------code----------------------
npm i expo-constants
-------------------code----------------------

---

## 5. Import Constants

-------------------code----------------------
import Constants from "expo-constants";
-------------------code----------------------

Important:

* No curly braces
* It is a default export

---

## 6. Use Constants for Status Bar Height

Replace the previous platform logic with:

-------------------code----------------------
const styles = StyleSheet.create({
  screen: {
    paddingTop: Constants.statusBarHeight,
  },
});
-------------------code----------------------

Why this works:

* On Android → returns 24px
* On iOS → returns 44px
* SafeAreaView already handles iOS safe area
* Extra padding does not negatively affect iOS

Now:

* Platform API is no longer needed
* StatusBar API is no longer needed
* Code is simpler

---

# Part 4: Avoid Repetition (Create Reusable Screen Component)

Problem:
Every new screen would require:

* Import SafeAreaView
* Import Constants
* Add padding style
* Apply styles

This is repetitive.

Solution:
Create a reusable `Screen` component.

---

# Part 5: Creating the Screen Component

## 7. Create a New File

-------------------code----------------------
components/Screen.js
-------------------code----------------------

---

## 8. Implement Screen Component

### Step 1: Imports

-------------------code----------------------
import React from "react";
import { SafeAreaView, StyleSheet } from "react-native";
import Constants from "expo-constants";
-------------------code----------------------

---

### Step 2: Create Styles

-------------------code----------------------
const styles = StyleSheet.create({
  screen: {
    paddingTop: Constants.statusBarHeight,
  },
});
-------------------code----------------------

---

### Step 3: Accept and Render Children

-------------------code----------------------
function Screen({ children }) {
  return (
    <SafeAreaView style={styles.screen}>
      {children}
    </SafeAreaView>
  );
}
-------------------code----------------------

This allows us to use it like `SafeAreaView`.

---

### Step 4: Export

-------------------code----------------------
export default Screen;
-------------------code----------------------

---

# Part 6: Using the Screen Component

## 9. Update MessagesScreen

### Step 1: Import Screen

-------------------code----------------------
import Screen from "../components/Screen";
-------------------code----------------------

### Step 2: Replace SafeAreaView

Before:

-------------------code----------------------
<SafeAreaView style={styles.screen}>
  <FlatList ... />
</SafeAreaView>
-------------------code----------------------

After:

-------------------code----------------------
<Screen>
  <FlatList ... />
</Screen>
-------------------code----------------------

Now:

* No SafeAreaView import needed
* No Constants import needed
* No screen style needed

Code is cleaner and reusable.

---

# 10. Final Code

---

## Screen.js

-------------------code----------------------
import React from "react";
import { SafeAreaView, StyleSheet } from "react-native";
import Constants from "expo-constants";

function Screen({ children }) {
  return (
    <SafeAreaView style={styles.screen}>
      {children}
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  screen: {
    paddingTop: Constants.statusBarHeight,
  },
});

export default Screen;
-------------------code----------------------

---

## MessagesScreen.js

-------------------code----------------------
import React from "react";
import { FlatList } from "react-native";
import Screen from "../components/Screen";
import ListItem from "../components/ListItem";

const messages = [
  {
    id: 1,
    title: "t1",
    description: "d1",
    image: require("../assets/mosh.jpg"),
  },
  {
    id: 2,
    title: "t2",
    description: "d2",
    image: require("../assets/mosh.jpg"),
  },
];

function MessagesScreen() {
  return (
    <Screen>
      <FlatList
        data={messages}
        keyExtractor={(message) => message.id.toString()}
        renderItem={({ item }) => (
          <ListItem
            title={item.title}
            subtitle={item.description}
            image={item.image}
          />
        )}
      />
    </Screen>
  );
}

export default MessagesScreen;
-------------------code----------------------

---

# 11. Best Practices

1. Always protect screens from status bar overlap.
2. Avoid duplicating platform logic in multiple screens.
3. Encapsulate repeated layout logic in reusable components.
4. Prefer `expo-constants` for clean status bar handling.
5. Keep screens clean and focused on layout.

---

# 12. Summary

In this lesson, we:

* Fixed the status bar overlap issue
* Learned that `SafeAreaView` alone is not enough for Android
* Used `expo-constants` to simplify platform detection
* Eliminated repeated code by creating a reusable `Screen` component
* Refactored `MessagesScreen` to use the new component

Result:
Cleaner, reusable, maintainable screen structure across the entire app.

Next step: Adding separators between list items.


*** 4- Separators ***

---

# Adding Separators and Padding to Lists in React Native

---

## 1. Problem: Adding a Separator Between List Items

We want a visual separator between items in a `FlatList`.

**Two possible approaches:**

1. **Add a border inside `ListItem`**

   * Pros: Easy to implement
   * Cons: Adds extra line after the last item, requires hacks to remove it → not recommended

2. **Use `FlatList`’s `ItemSeparatorComponent` prop**

   * Pros: Clean solution, no extra lines after the last item
   * Recommended approach

---

## 2. Using `ItemSeparatorComponent` in FlatList

### Step 1: Implement Inline Separator

-------------------code----------------------
<FlatList
  data={messages}
  keyExtractor={(message) => message.id.toString()}
  renderItem={({ item }) => (
    <ListItem
      title={item.title}
      subtitle={item.description}
      image={item.image}
    />
  )}
  ItemSeparatorComponent={() => (
    <View
      style={{
        width: "100%",
        height: 1,
        backgroundColor: "black",
      }}
    />
  )}
/>
-------------------code----------------------

Works: no separator after the last item

**Note:** Don't forget to import `View`:

-------------------code----------------------
import { View } from "react-native";
-------------------code----------------------

---

## 3. Making the Separator Reusable

### Step 1: Create a New Component

**File:** `components/ListItemSeparator.js`

-------------------code----------------------
import React from "react";
import { View, StyleSheet } from "react-native";
import colors from "../config/colors";

function ListItemSeparator() {
  return <View style={styles.separator} />;
}

const styles = StyleSheet.create({
  separator: {
    width: "100%",
    height: 1,
    backgroundColor: colors.light, // light gray from color palette
  },
});

export default ListItemSeparator;
-------------------code----------------------

### Step 2: Add Light Gray to Colors Palette

**File:** `config/colors.js`

-------------------code----------------------
export default {
  primary: "#fc5c65",
  secondary: "#4ecdc4",
  black: "#000",
  white: "#fff",
  medium: "#6e6969",
  light: "#f8f4f4", // added light gray
};
-------------------code----------------------

---

## 4. Use Reusable Separator in FlatList

-------------------code----------------------
import ListItemSeparator from "../components/ListItemSeparator";

<FlatList
  data={messages}
  keyExtractor={(message) => message.id.toString()}
  renderItem={({ item }) => (
    <ListItem
      title={item.title}
      subtitle={item.description}
      image={item.image}
    />
  )}
  ItemSeparatorComponent={ListItemSeparator}
/>
-------------------code----------------------

Cleaner and reusable across the app

---

## 5. Adding Padding to List Items

Currently, `ListItem` content is too close to the edges.

### Step 1: Add Padding in ListItem Container

**File:** `components/ListItem.js`

-------------------code----------------------
const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    padding: 15, // added padding
    alignItems: "center",
  },
  image: {
    width: 70,
    height: 70,
    borderRadius: 35,
    marginRight: 10,
  },
  detailsContainer: {
    flex: 1,
    justifyContent: "center",
  },
  title: {
    fontWeight: "500",
  },
  subtitle: {
    color: colors.medium,
  },
});
-------------------code----------------------

Now the list items look much better

---

# 6. Summary of Best Practices

1. **Never hardcode separators inside `ListItem`** → use `ItemSeparatorComponent`.
2. **Make separators reusable** for maintainability.
3. **Use colors from a palette** instead of hardcoding.
4. **Add padding inside items** to prevent content from touching screen edges.
5. `FlatList` props used:

   * `data` → array of items
   * `keyExtractor` → unique ID
   * `renderItem` → renders each list item
   * `ItemSeparatorComponent` → renders separator between items

---

# 7. Next Step

* Handling item selections in the list (tapping an item to navigate or interact).

---


*** 5- Handling Selections ***

---

# Handling Selections in List Items in React Native

---

## 1. Problem: List Items Are Not Selectable

Currently, our `ListItem` component does not respond when tapped.
We want a **visual feedback** and the ability to perform an action when a user selects an item.

---

## 2. Wrapping List Items in a Touchable Component

React Native provides multiple touchable components:

| Component                  | Description                                            |
| -------------------------- | ------------------------------------------------------ |
| `TouchableOpacity`         | Changes opacity briefly on tap; subtle visual feedback |
| `TouchableHighlight`       | Highlights item with a background color on tap         |
| `TouchableWithoutFeedback` | No visual feedback; just detects taps                  |

**Decision:** Use `TouchableHighlight` because we want a **background color change** when selecting a list item.

---

## 3. Implementing TouchableHighlight in ListItem

### Step 1: Wrap content with `TouchableHighlight`

-------------------code----------------------
import React from 'react';
import { View, Image, StyleSheet, TouchableHighlight } from 'react-native';
import AppText from './AppText';
import colors from '../config/colors';

function ListItem({ title, subtitle, image, onPress }) {
  return (
    <TouchableHighlight
      underlayColor={colors.light}
      onPress={onPress}
    >

      <View style={styles.container}>
        <Image source={image} style={styles.image} />

        <View>
          <AppText style={styles.title}>{title}</AppText>
          <AppText style={styles.subtitle}>{subtitle}</AppText>
        </View>
      </View>
    </TouchableHighlight>
  );
}

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    padding: 15,
  },
  image: {
    width: 70,
    height: 70,
    borderRadius: 35,
    marginRight: 10,
  },
  title: {
    fontWeight: '500',
  },
  subtitle: {
    color: colors.medium,
  },
});

export default ListItem;
-------------------code----------------------

---

## 4. Passing Selection Logic from Parent

The `ListItem` component should **not decide what happens on tap**.
Instead, it accepts an `onPress` prop from its parent (consumer).

### Step 1: Use `onPress` in MessagesScreen

-------------------code----------------------
import React from "react";
import { FlatList } from "react-native";
import Screen from "./Screen";
import ListItem from "../components/ListItem";
import ListItemSeparator from "../components/ListItemSeparator";

const messages = [
  {
    id: 1,
    title: "t1",
    description: "d1",
    image: require("../assets/mosh.jpg"),
  },
  {
    id: 2,
    title: "t2",
    description: "d2",
    image: require("../assets/mosh.jpg"),
  },
];

function MessagesScreen() {
  return (
    <Screen>
      <FlatList
        data={messages}
        keyExtractor={(message) => message.id.toString()}
        renderItem={({ item }) => (
          <ListItem
            title={item.title}
            subtitle={item.description}
            image={item.image}
            onPress={() => console.log("Message selected:", item)}
          />
        )}
        ItemSeparatorComponent={ListItemSeparator}
      />
    </Screen>
  );
}

export default MessagesScreen;
-------------------code----------------------

Now, when a list item is tapped:

* The background briefly changes to **light gray**
* The parent handles the action (e.g., logs the item, navigates to another screen)

---

## 5. Best Practices

1. **Use `TouchableHighlight` for list items** for proper visual feedback.
2. **Keep the `ListItem` component reusable**:

   * Don’t hardcode what happens on press
   * Pass an `onPress` prop from the parent
3. **Use colors from a palette** for consistency
4. **Encapsulate padding and styling** in `ListItem` for cleaner layout
5. **Combine with separators** for clean lists

---

## 6. Next Step

* Handle **swipes on list items** for actions like delete or archive.

---

This structure ensures **reusable, maintainable, and visually responsive list items**.

---


*** 6- Handling Swipes ***
# Building Swipeable List Items in React Native

## 1. Goal of This Lesson

Enhance the `ListItem` component so it:

1. Responds to swipe gestures.
2. Shows a red delete action when swiping left.
3. Keeps the component reusable.
4. Extracts the delete UI into a reusable component.

We will use **React Native Gesture Handler** and the `Swipeable` component.

---

# 2. Installing React Native Gesture Handler

## 2.1 Why Use Expo Install?

When using libraries that are part of the Expo ecosystem, always install them with:

-------------------code----------------------
expo install react-native-gesture-handler
-------------------code----------------------

### Why?

* Ensures compatibility with your current Expo SDK version.
* Under the hood, Expo still uses npm or yarn.
* Avoids version conflicts.

---

# 3. Understanding Swipeable

From the React Native Gesture Handler documentation:

* `Swipeable` allows swipe-to-left and swipe-to-right behavior.
* It exposes:

  * `renderRightActions`
  * `renderLeftActions`

In this lesson, we use:

-------------------code----------------------
renderRightActions
-------------------code----------------------

When the user swipes left, the right-side actions appear.

---

# 4. Step 1 – Wrap ListItem with Swipeable

## 4.1 Import Swipeable

In `ListItem.js`:

-------------------code----------------------
import Swipeable from "react-native-gesture-handler/Swipeable";
-------------------code----------------------

---

## 4.2 Wrap the Component

Instead of returning your content directly, wrap it:

-------------------code----------------------
<Swipeable renderRightActions={renderRightActions}>
  {/* Existing List Item Content */}
</Swipeable>
-------------------code----------------------

---

## 4.3 Keep ListItem Reusable

Do NOT hardcode the delete UI inside `ListItem`.

Instead:

1. Accept a prop called `renderRightActions`.
2. Pass it to `Swipeable`.

### Update ListItem Component

-------------------code----------------------
function ListItem({ title, subTitle, renderRightActions, onPress }) {
  return (
    <Swipeable renderRightActions={renderRightActions}>
      <TouchableHighlight
        underlayColor={colors.light}
        onPress={onPress}
      >
        <View style={styles.container}>
          <Text>{title}</Text>
          <Text>{subTitle}</Text>
        </View>
      </TouchableHighlight>
    </Swipeable>
  );
}
-------------------code----------------------

Now the consumer decides what appears on swipe.

---

# 5. Step 2 – Test Swipe in Messages Screen

In `MessagesScreen.js`, pass a temporary inline function:

-------------------code----------------------
renderRightActions={() => (
  <View
    style={{
      backgroundColor: "red",
      width: 70,
    }}
  />
)}
-------------------code----------------------

Test:

* Swipe left.
* You should see a red box.

This confirms `Swipeable` works.

---

# 6. Step 3 – Extract Delete Action into Reusable Component

We should not repeat inline red-box logic everywhere.

Create:

-------------------code----------------------
components/ListItemDeleteAction.js
-------------------code----------------------

---

# 7. Step 4 – Create Color Palette Update

In `config/colors.js`, add:

-------------------code----------------------
export default {
  primary: "#fc5c65",
  secondary: "#4ecdc4",
  white: "#fff",
  light: "#f8f4f4",
  danger: "#ff5252",
};
-------------------code----------------------

`danger` is a softer red.

---

# 8. Step 5 – Implement ListItemDeleteAction Component

## 8.1 Basic Structure

-------------------code----------------------
import React from "react";
import { View, StyleSheet } from "react-native";
import colors from "../config/colors";

function ListItemDeleteAction() {
  return <View style={styles.container} />;
}

const styles = StyleSheet.create({
  container: {
    backgroundColor: colors.danger,
    width: 70,
  },
});

export default ListItemDeleteAction;
-------------------code----------------------

---

# 9. Step 6 – Use Delete Component in Messages Screen

Instead of inline red view:

-------------------code----------------------
renderRightActions={ListItemDeleteAction}
-------------------code----------------------

Important:

We are passing a reference, NOT rendering it:

Correct:

-------------------code----------------------
renderRightActions={ListItemDeleteAction}
-------------------code----------------------

Wrong:

-------------------code----------------------
renderRightActions={<ListItemDeleteAction />}
-------------------code----------------------

---

# 10. Step 7 – Add Trash Icon

## 10.1 Import Icon

Inside `ListItemDeleteAction.js`:

-------------------code----------------------
import { MaterialCommunityIcons } from "@expo/vector-icons";
-------------------code----------------------

---

## 10.2 Add Icon

Update component:

-------------------code----------------------
function ListItemDeleteAction() {
  return (
    <View style={styles.container}>
      <MaterialCommunityIcons
        name="trash-can"
        size={35}
        color={colors.white}
      />
    </View>
  );
}
-------------------code----------------------

---

# 11. Step 8 – Center the Icon

Update styles:

-------------------code----------------------
const styles = StyleSheet.create({
  container: {
    backgroundColor: colors.danger,
    width: 70,
    justifyContent: "center",
    alignItems: "center",
  },
});
-------------------code----------------------

Now the trash icon is perfectly centered.

---

# 12. Full Final Code

## 12.1 config/colors.js

-------------------code----------------------
export default {
  primary: "#fc5c65",
  secondary: "#4ecdc4",
  white: "#fff",
  light: "#f8f4f4",
  danger: "#ff5252",
};
-------------------code----------------------

---

## 12.2 components/ListItemDeleteAction.js

-------------------code----------------------
import React from "react";
import { View, StyleSheet } from "react-native";
import { MaterialCommunityIcons } from "@expo/vector-icons";
import colors from "../config/colors";

function ListItemDeleteAction() {
  return (
    <View style={styles.container}>
      <MaterialCommunityIcons
        name="trash-can"
        size={35}
        color={colors.white}
      />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    backgroundColor: colors.danger,
    width: 70,
    justifyContent: "center",
    alignItems: "center",
  },
});

export default ListItemDeleteAction;
-------------------code----------------------

---

## 12.3 components/ListItem.js

-------------------code----------------------
import { Image, StyleSheet, TouchableHighlight, View } from 'react-native';
import Swipeable from "react-native-gesture-handler/Swipeable";
import colors from '../config/colors';
import AppText from './AppText';

function ListItem({ title, subtitle, image, onPress, renderRightActions }) {
  return (
    <Swipeable renderRightActions={renderRightActions}>
      <TouchableHighlight
        underlayColor={colors.light}
        onPress={onPress}
      >

        <View style={styles.container}>
          <Image source={image} style={styles.image} />

          <View>
            <AppText style={styles.title}>{title}</AppText>
            <AppText style={styles.subtitle}>{subtitle}</AppText>
          </View>
        </View>
      </TouchableHighlight>
    </Swipeable>
  );
}

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    padding: 15,
  },
  image: {
    width: 70,
    height: 70,
    borderRadius: 35,
    marginRight: 10,
  },
  title: {
    fontWeight: '500',
  },
  subtitle: {
    color: colors.medium,
  },
});

export default ListItem;
-------------------code----------------------

---

## 12.4 screens/MessagesScreen.js (Usage)

-------------------code----------------------
import React from "react";
import { FlatList } from "react-native";
import ListItem from "../components/ListItem";
import ListItemDeleteAction from "../components/ListItemDeleteAction";

function MessagesScreen() {
  return (
    <FlatList
      data={messages}
      keyExtractor={(item) => item.id.toString()}
      renderItem={({ item }) => (
        <ListItem
          title={item.title}
          subTitle={item.description}
          renderRightActions={ListItemDeleteAction}
          onPress={() => console.log("Message selected", item)}
        />
      )}
    />
  );
}
-------------------code----------------------


App.js
-------------------code----------------------
import MessagesScreen from "./app/screens/MessagesScreen";
import { GestureHandlerRootView } from 'react-native-gesture-handler';


export default function App() {
  return (<GestureHandlerRootView style={{ flex: 1 }}> <MessagesScreen /></GestureHandlerRootView>)

}
-------------------code----------------------
---

# 13. Important Notes and Best Practices

1. Always install Expo-supported libraries using `expo install`.
2. Keep components reusable:

   * Do not hardcode logic.
   * Accept props for behavior.
3. Extract repeated UI into separate components.
4. Use a centralized color palette.
5. Pass component references to `renderRightActions`, not JSX.
6. Keep styles clean and consistent.

---

# 14. Summary

In this lesson, you learned how to:

1. Install and use React Native Gesture Handler.
2. Wrap a component with `Swipeable`.
3. Use `renderRightActions` to show actions on swipe.
4. Keep components reusable by passing behavior via props.
5. Extract delete UI into a reusable component.
6. Add and style icons using Expo vector icons.
7. Center elements properly using flexbox.

You now have a professional, reusable swipe-to-delete list item pattern that can be reused across your app.


*** 7- Deleting an Item ***
# Deleting Items from a Swipeable List (React Native)

This lecture explains how to delete a list item when the user taps the red swipe action. We will:

1. Make the delete action touchable
2. Keep the component reusable
3. Move delete logic to the screen
4. Use `useState` (React Hooks)
5. Remove the item using `filter`
6. Trigger re-render correctly

---

# 1. Making the Delete Action Touchable

We are inside:

-------------------code----------------------
ListItemDeleteAction.js
-------------------code----------------------

To detect taps on the red delete box, we wrap it with a touchable component.

## Why Not TouchableOpacity or TouchableHighlight?

In this context:

* Opacity effect looks strange
* Highlight effect looks strange

Best choice here:

-------------------code----------------------
TouchableWithoutFeedback
-------------------code----------------------

---

## Step 1 – Wrap the View

-------------------code----------------------
import { StyleSheet, View, TouchableWithoutFeedback } from "react-native";
import { MaterialCommunityIcons } from "@expo/vector-icons";
import colors from "../config/colors";

function ListItemDeleteAction({ onPress }) {
  return (
    <TouchableWithoutFeedback onPress={onPress}>
      <View style={styles.container}>
        <MaterialCommunityIcons
          name="trash-can"
          size={35}
          color={colors.white}
        />
      </View>
    </TouchableWithoutFeedback>
  );
}
-------------------code----------------------

---

# 2. Keep the Component Reusable

Important principle:

The delete action component should NOT contain deletion logic.

Why?

Because:

* Today it deletes messages
* Tomorrow it may delete notes
* The behavior must be controlled by the parent

So we pass:

-------------------code----------------------
onPress
-------------------code----------------------

as a prop and delegate the action.

---

# 3. Passing onPress from MessagesScreen

In `MessagesScreen.js`, you cannot pass props directly like this:

-------------------code----------------------
renderRightActions={ListItemDeleteAction}
-------------------code----------------------

Because you need to attach `onPress`.

Instead, pass a function that renders the component.

---

## Step 2 – Update renderRightActions

-------------------code----------------------
renderRightActions={() => (
  <ListItemDeleteAction
    onPress={() => handleDelete(item)}
  />
)}
-------------------code----------------------

Now we can control what happens when the delete button is pressed.

---

# 4. Why We Need State

If we remove an item from the array directly:

-------------------code----------------------
messages.splice(...)
-------------------code----------------------

The component will NOT re-render.

Why?

Because React only re-renders when state changes.

Solution:

Use the `useState` hook.

---

# 5. Using useState (React Hook)

## Step 3 – Import useState

-------------------code----------------------
import React, { useState } from "react";
-------------------code----------------------

---

## Step 4 – Rename Static Array

Instead of:

-------------------code----------------------
const messages = [...]
-------------------code----------------------

Use:

-------------------code----------------------
const initialMessages = [...]
-------------------code----------------------

---

## Step 5 – Declare State

-------------------code----------------------
const [messages, setMessages] = useState(initialMessages);
-------------------code----------------------

Explanation:

* `messages` → current state
* `setMessages` → function to update state
* `initialMessages` → initial value

Whenever `setMessages` is called → component re-renders.

---

# 6. Implement handleDelete

## Step 6 – Create Delete Function

-------------------code----------------------
const handleDelete = (message) => {
  setMessages(messages.filter(m => m.id !== message.id));
};
-------------------code----------------------

Explanation:

* `filter()` creates a NEW array
* Keeps all items except the deleted one
* Updating state triggers re-render

Important:

Never mutate state directly.

Bad:

-------------------code----------------------
messages.splice(...)
-------------------code----------------------

Good:

-------------------code----------------------
filter()
-------------------code----------------------

Always return a new array.

---

# 7. Use handleDelete

Replace console log:

-------------------code----------------------
renderRightActions={() => (
  <ListItemDeleteAction
    onPress={() => handleDelete(item)}
  />
)}
-------------------code----------------------

Now tapping delete removes the item visually.

---

# Final Code

---

## ListItemDeleteAction.js

-------------------code----------------------
import { StyleSheet, View, TouchableWithoutFeedback } from "react-native";
import { MaterialCommunityIcons } from "@expo/vector-icons";
import colors from "../config/colors";

function ListItemDeleteAction({ onPress }) {
  return (
    <TouchableWithoutFeedback onPress={onPress}>
      <View style={styles.container}>
        <MaterialCommunityIcons
          name="trash-can"
          size={35}
          color={colors.white}
        />
      </View>
    </TouchableWithoutFeedback>
  );
}

const styles = StyleSheet.create({
  container: {
    backgroundColor: colors.danger,
    width: 70,
    justifyContent: "center",
    alignItems: "center",
  },
});

export default ListItemDeleteAction;
-------------------code----------------------

---

## MessagesScreen.js

-------------------code----------------------
import React, { useState } from "react";
import { FlatList } from "react-native";
import ListItem from "../components/ListItem";
import ListItemDeleteAction from "../components/ListItemDeleteAction";
import ListItemSeparator from "../components/ListItemSeparator";
import Screen from "./Screen";

const initialMessages = [
  {
    id: 1,
    title: "t1",
    description: "d1",
    image: require("../assets/mosh.jpg"),
  },
  {
    id: 2,
    title: "t2",
    description: "d2",
    image: require("../assets/mosh.jpg"),
  },
];

function MessagesScreen() {
  const [messages, setMessages] = useState(initialMessages);

  const handleDelete = (message) => {
    setMessages(messages.filter(m => m.id !== message.id));
  };

  return (
    <Screen>
      <FlatList
        data={messages}
        keyExtractor={(message) => message.id.toString()}
        renderItem={({ item }) => (
          <ListItem
            title={item.title}
            subtitle={item.description}
            image={item.image}
            onPress={() => console.log("Message selected:", item)}
            renderRightActions={() => (
              <ListItemDeleteAction
                onPress={() => handleDelete(item)}
              />
            )}
          />
        )}
        ItemSeparatorComponent={ListItemSeparator}
      />
    </Screen>
  );
}

export default MessagesScreen;
-------------------code----------------------

---

# Important Best Practices

1. Never mutate state directly.
2. Always use setState (or setMessages).
3. Use filter() to remove items.
4. Keep components reusable.
5. Delegate logic to parent components.
6. Hooks only work inside function components.
7. Any function starting with "use" is a Hook.

---

# What Happens Internally

When you call:

-------------------code----------------------
setMessages(newArray)
-------------------code----------------------

React:

1. Updates state
2. Re-runs the component function
3. Re-renders UI
4. FlatList receives new data
5. Removed item disappears

---

# Summary

In this lecture you learned:

* How to make the swipe delete button clickable
* Why deletion logic belongs in the screen component
* How to use `useState` hook
* How to update state correctly
* Why `filter()` is used to remove items
* How React re-renders when state changes

You now understand:

Reusable components
State management with hooks
Proper deletion pattern in React Native

Next concept: Pull to refresh.

*** 8- Implementing Pull to Refresh ***
# Implementing Pull to Refresh in React Native

In this lecture, we implement pull-to-refresh using `FlatList`, fix a layout issue, and explain why Hooks must be declared at the top of function components.

---

# 1. What is Pull to Refresh?

Pull to refresh allows users to:

* Drag the list downward
* Trigger a refresh action
* Reload updated data (usually from a backend)

React Native makes this very simple using two props:

* `refreshing`
* `onRefresh`

---

# 2. Add a Refreshing State

Pull to refresh needs a boolean state to indicate whether the list is refreshing.

## Step 1 – Import useState

-------------------code----------------------
import React, { useState } from "react";
-------------------code----------------------

---

## Step 2 – Declare refreshing State

Hooks must be declared at the top of the component.

-------------------code----------------------
const [refreshing, setRefreshing] = useState(false);
-------------------code----------------------

Explanation:

* `refreshing` → current boolean state
* `setRefreshing` → function to update it
* `false` → initial value (not refreshing)

---

# 3. Connect refreshing to FlatList

## Step 3 – Bind refreshing Prop

-------------------code----------------------
<FlatList
  data={messages}
  refreshing={refreshing}
  ...
/>
-------------------code----------------------

Now FlatList knows whether it should show the loading indicator.

---

# 4. Implement onRefresh

## Step 4 – Add onRefresh Function

-------------------code----------------------
<FlatList
  data={messages}
  refreshing={refreshing}
  onRefresh={() => {
    setMessages([initialMessages[1]]);
  }}
/>
-------------------code----------------------

In a real app:

* You would call an API
* Fetch new messages
* Update state

Here we simulate it by replacing the array.

When you pull down:

* First item disappears
* List updates

---

# 5. Fix Layout Cut-Off Issue

After implementing pull to refresh, a visual issue appears:

* Screen does not take full height
* Pull gesture shows a cut-off area

Why?

Because the `Screen` component does not fill the entire screen.

---

# 6. Fix Screen Component

## Step 5 – Add flex: 1

Inside `Screen.js`:

-------------------code----------------------
const styles = StyleSheet.create({
  screen: {
    paddingTop: Constants.statusBarHeight,
    flex: 1,
  },
});
-------------------code----------------------

Why `flex: 1`?

It tells the component:

Take the entire available screen space.

After adding this:

* No more cut-off
* Smooth pull-to-refresh behavior
* Layout behaves correctly

---

# 7. Why Hooks Must Be at the Top

You asked:

Why must Hooks be called at the beginning of the component?

This is one of the fundamental rules of Hooks.

---

## Rule of Hooks

Hooks must:

1. Be called at the top level of a function component
2. Never be inside loops
3. Never be inside conditions
4. Never be inside nested functions

---

## Why?

React relies on the order of Hooks.

Example:

Correct:

-------------------code----------------------
function Example() {
  const [count, setCount] = useState(0);
  const [name, setName] = useState("");

  return null;
}
-------------------code----------------------

React remembers:

Hook #1 → count
Hook #2 → name

Now imagine this:

Wrong:

-------------------code----------------------
function Example() {
  if (someCondition) {
    const [count, setCount] = useState(0);
  }

  const [name, setName] = useState("");

  return null;
}
-------------------code----------------------

If `someCondition` changes between renders:

The order of Hooks changes.

React gets confused because it matches Hooks by position, not by name.

This can cause:

* State corruption
* Unexpected behavior
* Crashes

That’s why Hooks must always:

Be at the top.
Always run in the same order.

---

# 8. Final Code

---

## MessagesScreen.js

-------------------code----------------------
import React, { useState } from "react";
import { FlatList } from "react-native";
import ListItem from "../components/ListItem";
import ListItemDeleteAction from "../components/ListItemDeleteAction";
import ListItemSeparator from "../components/ListItemSeparator";
import Screen from "./Screen";

const initialMessages = [
  {
    id: 1,
    title: "t1",
    description: "d1",
    image: require("../assets/mosh.jpg"),
  },
  {
    id: 2,
    title: "t2",
    description: "d2",
    image: require("../assets/mosh.jpg"),
  },
];

function MessagesScreen() {
  const [messages, setMessages] = useState(initialMessages);
  const [refreshing, setRefreshing] = useState(false);

  const handleDelete = (message) => {
    setMessages(messages.filter(m => m.id !== message.id));
  };

  return (
    <Screen>
      <FlatList
        data={messages}
        keyExtractor={(message) => message.id.toString()}
        refreshing={refreshing}
        onRefresh={() => {
          setMessages([initialMessages[1]]);
        }}
        renderItem={({ item }) => (
          <ListItem
            title={item.title}
            subtitle={item.description}
            image={item.image}
            onPress={() => console.log("Message selected:", item)}
            renderRightActions={() => (
              <ListItemDeleteAction
                onPress={() => handleDelete(item)}
              />
            )}
          />
        )}
        ItemSeparatorComponent={ListItemSeparator}
      />
    </Screen>
  );
}

export default MessagesScreen;
-------------------code----------------------

---

## Screen.js

-------------------code----------------------
import { SafeAreaView, StyleSheet } from "react-native";
import Constants from "expo-constants";

function Screen({ children }) {
  return (
    <SafeAreaView style={styles.screen}>
      {children}
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  screen: {
    paddingTop: Constants.statusBarHeight,
    flex: 1,
  },
});

export default Screen;
-------------------code----------------------

---

# Important Best Practices

1. Always declare Hooks at the top.
2. Never conditionally call Hooks.
3. Always use state when UI must re-render.
4. Use `flex: 1` for full-screen layouts.
5. Simulate backend refresh for now, but later replace with API calls.
6. Keep logic inside the screen, not inside reusable components.

---

# Summary

In this lecture you learned:

* How to implement pull to refresh using `FlatList`
* How to manage a refreshing boolean state
* How to simulate refreshing behavior
* Why `flex: 1` is necessary for full-screen layouts
* Why Hooks must always be declared at the top
* How React tracks Hooks by order

You now understand:

State management with multiple Hooks
Pull-to-refresh implementation
React Hook rules and reasoning
Layout correction using flex

Next logical step: connecting refresh to a backend API.

*** 10- Planning the Account Screen ***
# Planning the My Account Screen

In this lecture, we plan how to build the **My Account** screen and define the development strategy step by step.

The focus is not just on implementation, but on thinking like a software engineer:
Break complex UI into small, reusable building blocks.

---

# 1. Understanding the Target Layout

The **My Account** screen will contain:

1. A single list item at the top (user profile)
2. A group of menu items in the middle (inside a FlatList)
3. A single list item at the bottom (e.g., logout)

So the structure is:

* Independent List Item (top)
* FlatList (middle section)
* Independent List Item (bottom)

---

# 2. Two Possible Implementation Approaches

## Option 1 — Use SectionList

React Native provides `SectionList`, which:

* Supports section headers
* Supports section separators
* Has all FlatList features

Example structure:

-------------------code----------------------
<SectionList
  sections={[
    { title: "Profile", data: [...] },
    { title: "Menu", data: [...] },
    { title: "Logout", data: [...] },
  ]}
  renderItem={...}
  renderSectionHeader={...}
/>
-------------------code----------------------

Why not use it?

* Overkill for a simple layout
* Adds unnecessary complexity
* Harder to customize spacing exactly like the design

---

## Option 2 — Simpler and More Flexible (Chosen Approach)

We will:

1. Render a ListItem manually (top)
2. Wrap it inside a container with margin
3. Render a FlatList (middle section)
4. Wrap it inside another container with margin
5. Render one more independent ListItem (bottom)

This gives:

* More control over spacing
* Simpler logic
* Easier customization

---

# 3. Final Screen Structure (Conceptual)

-------------------code----------------------
<Screen>

  <View style={styles.container}>
    <ListItem />
  </View>

  <View style={styles.container}>
    <FlatList />
  </View>

  <ListItem />

</Screen>
-------------------code----------------------

So we are mixing:

* Standalone components
* A FlatList in the middle

---

# 4. Development Strategy (Step-by-Step Plan)

Instead of building the full screen immediately, we will:

## Step 1 — Build a Reusable Icon Component

We need an icon that looks like:

* Colored background
* Rounded shape
* Icon centered inside

This will be reusable across the app.

---

## Step 2 — Extend ListItem

Currently, `ListItem` only supports:

* image
* title
* subtitle

We will extend it to support:

* icon
* custom icon background color

So it can render:

Either an image
Or an icon

This makes `ListItem` more flexible.

---

## Step 3 — Build the MyAccountScreen

Once we have:

* Reusable Icon component
* Extended ListItem

We combine everything to build:

* Profile section
* Menu section
* Logout item

---

# 5. Why This Approach Is Important

This lecture emphasizes an important mindset:

## Always Build in Small Steps

Instead of:

Building everything at once

We:

1. Identify reusable components
2. Build them independently
3. Test them
4. Combine them

---

# 6. Engineering Mindset

Good software development means:

* Divide complex UI into small parts
* Build and test each part
* Make components reusable
* Avoid duplication
* Keep screens clean and simple

This is how scalable apps are built.

---

# 7. Key Design Decisions

* Avoid SectionList for simple layout
* Use containers with margin for spacing
* Separate UI building blocks from screen logic
* Build reusable components first

---

# 8. What Comes Next

Next step:

Build a reusable Icon component that:

* Accepts icon name
* Accepts background color
* Has consistent size
* Centers the icon properly

After that:

Extend ListItem to support icons.

---

# Summary

In this lecture, we:

* Analyzed the My Account screen layout
* Compared SectionList vs custom structure
* Chose a simpler and more flexible approach
* Planned development in small steps
* Identified reusable components to build first
* Learned the importance of incremental development

Core takeaway:

Break large UI into small reusable pieces.
Build, test, then combine.

This mindset leads to clean, scalable applications.

*** 11- Building the Icon Component ***
# Building a Reusable Icon Component in React Native

## 1. Goal of This Lecture

In this lecture, we:

1. Replace the existing screen content with a reusable `Screen` component.
2. Create a reusable `Icon` component.
3. Support dynamic props (name, size, background color, icon color).
4. Add default values for easier usage.
5. Center the icon inside a circular container.
6. Test different configurations.

This is preparation for extending the `ListItem` component later.

---

# 2. Step 1 – Render a Basic Screen Component

First, remove the previous screen content and render a basic `Screen` component.

## App.js

-------------------code----------------------
import React from "react";
import Screen from "./app/components/Screen";

function App() {
  return <Screen />;
}

export default App;
-------------------code----------------------

Important:

* VS Code may not auto-import `Screen`. Import it manually.
* The `Screen` component gives us proper padding (especially top padding).

---

# 3. Step 2 – Plan the Icon Component API

We want to use the `Icon` component like this:

-------------------code----------------------
<Icon
  name="email"
  size={50}
  backgroundColor="red"
  iconColor="white"
/>
-------------------code----------------------

The component should support:

* `name` → icon name
* `size` → circle size
* `backgroundColor` → circle color
* `iconColor` → icon color

We also want default values:

* `size = 40`
* `backgroundColor = "black"`
* `iconColor = "white"`

---

# 4. Step 3 – Create the Icon Component

Create a new file:

-------------------code----------------------
app/components/Icon.js
-------------------code----------------------

---

## 4.1 Import Dependencies

-------------------code----------------------
import React from "react";
import { View, StyleSheet } from "react-native";
import { MaterialCommunityIcons } from "@expo/vector-icons";
-------------------code----------------------

We use:

* `View` → container (circle)
* `MaterialCommunityIcons` → icon library

---

## 4.2 Define the Component with Default Props

-------------------code----------------------
function Icon({
  name,
  size = 40,
  backgroundColor = "black",
  iconColor = "white",
}) {
-------------------code----------------------

Using default values makes the component easier to use and more reusable.

---

## 4.3 Create the Circular Container

We need dynamic styles based on `size` and `backgroundColor`.

-------------------code----------------------
  return (
    <View
      style={[
        styles.container,
        {
          width: size,
          height: size,
          borderRadius: size / 2,
          backgroundColor,
        },
      ]}
    >
-------------------code----------------------

Why:

* `width = size`
* `height = size`
* `borderRadius = size / 2` → perfect circle
* `backgroundColor` → dynamic

---

## 4.4 Add the Icon Inside

-------------------code----------------------
      <MaterialCommunityIcons
        name={name}
        size={size * 0.5}
        color={iconColor}
      />
    </View>
  );
}
-------------------code----------------------

We use:

* `name` → icon name
* `size * 0.5` → icon is half the circle
* `color = iconColor`

---

## 4.5 Add Centering Styles

To center content inside a container:

* `justifyContent`
* `alignItems`

-------------------code----------------------
const styles = StyleSheet.create({
  container: {
    justifyContent: "center",
    alignItems: "center",
  },
});
-------------------code----------------------

This centers the icon perfectly inside the circle.

---

## 4.6 Export the Component

-------------------code----------------------
export default Icon;
-------------------code----------------------

---

# 5. Step 4 – Test the Icon Component

Update `App.js`:

-------------------code----------------------
import React from "react";
import Screen from "./app/components/Screen";
import Icon from "./app/components/Icon";

function App() {
  return (
    <Screen>
      <Icon
        name="email"
        size={50}
        backgroundColor="red"
        iconColor="white"
      />
    </Screen>
  );
}

export default App;
-------------------code----------------------

---

# 6. Testing Default Values

Try removing some props:

-------------------code----------------------
<Icon name="email" />
-------------------code----------------------

Expected result:

* Smaller icon (size 40)
* Black circle
* White icon

Try:

-------------------code----------------------
<Icon name="email" size={100} />
-------------------code----------------------

The circle scales correctly.

This confirms:

* Dynamic sizing works
* Default props work
* Centering works

---

# 7. Important Concepts Explained

## 7.1 Dynamic Styling

In React Native:

-------------------code----------------------
style={[
  styles.container,
  {
    width: size,
    height: size,
  },
]}
-------------------code----------------------

You can combine:

* Static styles
* Dynamic inline styles

This is the correct pattern for reusable components.

---

## 7.2 Why Default Props Matter

Default props:

* Reduce repetitive code
* Prevent bugs
* Make the component easier to use

Without defaults, every usage would require 4 props.

---

## 7.3 Why This Component Is Reusable

This icon component:

* Is not tied to branding
* Does not depend on app theme
* Accepts flexible configuration

This makes it portable across screens and projects.

---

# 8. Full Final Code

## app/components/Icon.js

-------------------code----------------------
import React from "react";
import { View, StyleSheet } from "react-native";
import { MaterialCommunityIcons } from "@expo/vector-icons";

function Icon({
  name,
  size = 40,
  backgroundColor = "black",
  iconColor = "white",
}) {
  return (
    <View
      style={[
        styles.container,
        {
          width: size,
          height: size,
          borderRadius: size / 2,
          backgroundColor,
        },
      ]}
    >
      <MaterialCommunityIcons
        name={name}
        size={size * 0.5}
        color={iconColor}
      />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    justifyContent: "center",
    alignItems: "center",
  },
});

export default Icon;
-------------------code----------------------

---

## App.js

-------------------code----------------------
import React from "react";
import Screen from "./app/components/Screen";
import Icon from "./app/components/Icon";

function App() {
  return (
    <Screen>
      <Icon
        name="email"
        size={50}
        backgroundColor="red"
        iconColor="white"
      />
    </Screen>
  );
}

export default App;
-------------------code----------------------

---

# 9. Best Practices

1. Always break work into small steps.
2. Build reusable components early.
3. Use default props for better DX (developer experience).
4. Keep components independent from branding unless necessary.
5. Test different prop combinations before moving forward.

---

# 10. Summary

In this lecture, we:

1. Replaced the main screen content with a `Screen` component.
2. Designed and implemented a reusable `Icon` component.
3. Used dynamic styling based on props.
4. Added default values for flexibility.
5. Centered the icon using `justifyContent` and `alignItems`.
6. Tested multiple configurations.

We now have a clean, reusable `Icon` component that we can use inside `ListItem` in the next step.

This follows a professional development mindset:

* Divide work into small pieces.
* Build, test, confirm.
* Then move to the next layer.

*** 12- Extending the ListItem Component ***
# React Native Lecture Notes: Extending ListItem to Support Icons and Conditional Rendering

## 1. Introduction

In this lecture, the goal is to improve the `ListItem` component to make it more reusable and flexible by:

1. Supporting icons in addition to images.
2. Removing empty space when no image exists.
3. Improving layout when subtitle is missing.
4. Avoiding unnecessary props and duplication.

---

# 2. Problem 1: Empty Space When Image is Missing

## Explanation

Previously, the `ListItem` always rendered an `Image`.

Even if no image was provided, React Native still reserved space because of:

-------------------code----------------------
width: 70
-------------------code----------------------

This created unwanted blank space.

---

## Solution: Conditional Rendering

Render the image only if it exists.

### Step 1: Fix Image Rendering

### Before

-------------------code----------------------
<Image source={image} style={styles.image} />
-------------------code----------------------

### After

-------------------code----------------------
{image && <Image source={image} style={styles.image} />}
-------------------code----------------------

This means:

* If image exists → render it
* If not → render nothing

---

# 3. Problem 2: Adding Icon Support Without Complicating Props

## Bad Approach (Not Recommended)

Adding multiple props like:

-------------------code----------------------
iconName
iconSize
iconColor
iconBackgroundColor
-------------------code----------------------

This makes component harder to use.

---

## Better Approach: Pass a Component Instead

Introduce a new prop:

-------------------code----------------------
ImageComponent
-------------------code----------------------

Naming rule:

Capital letter because it is a component, not data.

---

# 4. Step 2: Render ImageComponent

Add this inside container:

-------------------code----------------------
{ImageComponent}
-------------------code----------------------

Full example:

-------------------code----------------------
<View style={styles.container}>

  {ImageComponent}

  {image && <Image source={image} style={styles.image} />}

  <View style={styles.detailsContainer}>
    ...
  </View>

</View>
-------------------code----------------------

If ImageComponent is not provided → nothing renders.

---

# 5. Step 3: Use ImageComponent in App.js

Example:

-------------------code----------------------
<ListItem
  title="My Title"
  subtitle="My Subtitle"
  ImageComponent={
    <Icon
      name="email"
      size={40}
      backgroundColor="red"
      iconColor="white"
    />
  }
/>
-------------------code----------------------

Now ListItem supports icons.

---

# 6. Problem 3: Avoid Duplicate Margin

Previously margin was applied to image:

-------------------code----------------------
marginRight: 10
-------------------code----------------------

But icons also need spacing.

Instead of duplicating margin, apply margin to text container.

---

## Step 4: Add detailsContainer Style

-------------------code----------------------
detailsContainer: {
  marginLeft: 10,
}
-------------------code----------------------

---

## Apply it

-------------------code----------------------
<View style={styles.detailsContainer}>
-------------------code----------------------

---

## Remove marginRight from image

Before:

-------------------code----------------------
marginRight: 10
-------------------code----------------------

After:

Remove it.

---

# 7. Problem 4: Fix Layout When Subtitle is Missing

If subtitle does not exist, title should be centered vertically.

---

## Step 5: Add justifyContent

-------------------code----------------------
detailsContainer: {
  marginLeft: 10,
  justifyContent: "center",
}
-------------------code----------------------

---

# 8. Step 6: Conditionally Render Subtitle

Before:

-------------------code----------------------
<AppText style={styles.subtitle}>{subtitle}</AppText>
-------------------code----------------------

After:

-------------------code----------------------
{subtitle && (
  <AppText style={styles.subtitle}>{subtitle}</AppText>
)}
-------------------code----------------------

Now subtitle renders only when exists.

---

# 9. Updated ListItem Code Step-by-Step

## Updated Component

-------------------code----------------------
function ListItem({
  title,
  subtitle,
  image,
  ImageComponent,
  onPress,
  renderRightActions
}) {
-------------------code----------------------

---

## Container Layout

-------------------code----------------------
<View style={styles.container}>

  {ImageComponent}

  {image && (
    <Image source={image} style={styles.image} />
  )}

  <View style={styles.detailsContainer}>

    <AppText style={styles.title}>
      {title}
    </AppText>

    {subtitle && (
      <AppText style={styles.subtitle}>
        {subtitle}
      </AppText>
    )}

  </View>

</View>
-------------------code----------------------

---

# 10. Full Final Code (Complete ListItem)

-------------------code----------------------
import { Image, StyleSheet, TouchableHighlight, View } from 'react-native';
import Swipeable from "react-native-gesture-handler/Swipeable";

import colors from '../config/colors';
import AppText from './AppText';

function ListItem({
  title,
  subtitle,
  image,
  ImageComponent,
  onPress,
  renderRightActions
}) {
  return (

    <Swipeable renderRightActions={renderRightActions}>

      <TouchableHighlight
        underlayColor={colors.light}
        onPress={onPress}
      >

        <View style={styles.container}>

          {ImageComponent}

          {image && (
            <Image
              source={image}
              style={styles.image}
            />
          )}

          <View style={styles.detailsContainer}>

            <AppText style={styles.title}>
              {title}
            </AppText>

            {subtitle && (
              <AppText style={styles.subtitle}>
                {subtitle}
              </AppText>
            )}

          </View>

        </View>

      </TouchableHighlight>

    </Swipeable>

  );
}

const styles = StyleSheet.create({

  container: {
    flexDirection: 'row',
    padding: 15,
  },

  detailsContainer: {
    marginLeft: 10,
    justifyContent: "center",
  },

  image: {
    width: 70,
    height: 70,
    borderRadius: 35,
  },

  title: {
    fontWeight: '500',
  },

  subtitle: {
    color: colors.medium,
  },

});

export default ListItem;
-------------------code----------------------

App.js
-------------------code----------------------
import { GestureHandlerRootView } from 'react-native-gesture-handler';
import ListItem from "./app/components/ListItem";
import Screen from "./app/screens/Screen.js";
import Icon from "./app/components/Icon.js";

export default function App() {
  return (<GestureHandlerRootView style={{ flex: 1 }}>
    <Screen>
      <ListItem
        title="My title"
        subtitle="My subtitle"
        ImageComponent={<Icon name="email" />} />
    </Screen>
  </GestureHandlerRootView>)
}
-------------------code----------------------
---

# 11. Best Practices

## 1. Use Conditional Rendering

Always avoid rendering unused UI.

Example:

-------------------code----------------------
image && <Image />
-------------------code----------------------

---

## 2. Pass Components Instead of Many Props

Better:

-------------------code----------------------
ImageComponent={<Icon />}
-------------------code----------------------

Avoid:

-------------------code----------------------
iconName
iconSize
iconColor
-------------------code----------------------

---

## 3. Avoid Style Duplication

Use shared containers.

Bad:

margin on multiple elements

Good:

margin on parent container

---

## 4. Make Components Reusable

Reusable components:

Work in multiple situations

Support optional props

---

# 12. Visual Structure

Final Layout:

-------------------code----------------------
[ Icon or Image ]   Title
                    Subtitle
-------------------code----------------------

Or:

-------------------code----------------------
[ Icon ]   Title
-------------------code----------------------

Centered correctly.

---

# 13. Summary

In this lecture you learned:

1. How to conditionally render image
2. How to support icons using ImageComponent prop
3. How to remove unwanted empty space
4. How to center content when subtitle missing
5. How to write more reusable components
6. How to avoid duplicated styles

Final Result:

A fully reusable professional ListItem component that supports:

• Image
• Icon
• Title only
• Title + Subtitle
• Swipe actions

---


NOTE:

Error
-------------------code----------------------
[Error: PanGestureHandler must be used as a descendant of GestureHandlerRootView. 
Otherwise the gestures will not be recognized. 
See https://docs.swmansion.com/react-native-gesture-handler/docs/fundamentals/installation for more details.]
-------------------code----------------------

Solution:
This error happens because you're using Swipeable from react-native-gesture-handler, but your app is not wrapped with GestureHandlerRootView.

Swipeable internally uses PanGestureHandler, and it only works if the root of your app is GestureHandlerRootView.

This library is developed by Software Mansion.

This error happens because you're using `Swipeable` from **react-native-gesture-handler**, but your app is not wrapped with `GestureHandlerRootView`.

`Swipeable` internally uses `PanGestureHandler`, and it **only works if the root of your app is GestureHandlerRootView**.

This library is developed by Software Mansion.

---

# Why this error happens

You probably have:

-------------------code----------------------
import Swipeable from "react-native-gesture-handler/Swipeable";
-------------------code----------------------

But your `App.js` looks like this:

-------------------code----------------------
export default function App() {
  return <AccountScreen />;
}
-------------------code----------------------

This is missing the required root wrapper.

---

# Fix (Required)

## Step 1: Import GestureHandlerRootView

In App.js:

-------------------code----------------------
import { GestureHandlerRootView } from 'react-native-gesture-handler';
-------------------code----------------------

---

## Step 2: Wrap your entire app

Correct code:

-------------------code----------------------
import React from "react";
import { GestureHandlerRootView } from 'react-native-gesture-handler';

import AccountScreen from "./app/screens/AccountScreen";

export default function App() {

  return (

    <GestureHandlerRootView style={{ flex: 1 }}>

      <AccountScreen />

    </GestureHandlerRootView>

  );

}
-------------------code----------------------

---

# Why flex: 1 is important

Without it, your app may render blank screen.

Always include:

-------------------code----------------------
style={{ flex: 1 }}
-------------------code----------------------

---

# After Fix

Your Swipeable will work:

• swipe left
• swipe right
• gestures recognized

---

# Common mistake beginners make

Wrong:

-------------------code----------------------
<Screen>
  <ListItem
    title="My title"
    subtitle="My subtitle"
    ImageComponent={<Icon name="email" />} />
</Screen>
-------------------code----------------------

Correct:

-------------------code----------------------
<GestureHandlerRootView style={{ flex: 1 }}>
  <Screen>
    <ListItem
      title="My title"
      subtitle="My subtitle"
      ImageComponent={<Icon name="email" />} />
  </Screen>
</GestureHandlerRootView>
-------------------code----------------------

---

# Additional Important Note (If using Expo)

No extra installation needed.

If using bare React Native:

Run:

-------------------code----------------------bash
npm install react-native-gesture-handler
-------------------code----------------------

then:

-------------------code----------------------bash
npx pod-install
-------------------code----------------------

---

# Summary

Error cause:

GestureHandlerRootView missing

Fix:

Wrap your root app:

-------------------code----------------------
import { GestureHandlerRootView } from 'react-native-gesture-handler';
import ListItem from "./app/components/ListItem";
import Screen from "./app/screens/Screen.js";
import Icon from "./app/components/Icon.js";

export default function App() {
  return (<GestureHandlerRootView style={{ flex: 1 }}>
    <Screen>
      <ListItem
        title="My title"
        subtitle="My subtitle"
        ImageComponent={<Icon name="email" />} />
    </Screen>
  </GestureHandlerRootView>)
}
-------------------code----------------------

---
