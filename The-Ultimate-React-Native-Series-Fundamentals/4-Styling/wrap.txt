*** 1- Introduction ***
### Lecture Notes: Styling and Building User Interfaces

#### 1. Overview of the Section

This section focuses on improving the visual design and user experience of the application. The main objective is to enhance the appearance of the app by applying styling techniques and building reusable UI components.

#### 2. Key Learning Objectives

##### A. Building Better User Interfaces

* Learn how to design visually appealing and user-friendly screens.
* Improve the overall layout and presentation of existing screens.
* Implement new screens with improved styling and structure.

##### B. Reusable Components

* Create reusable UI components to avoid duplication.
* Improve maintainability and consistency across the application.
* Encourage modular and scalable design practices.

##### C. Style Properties

* Learn how to use different styling properties to control layout, spacing, colors, and typography.
* Understand how styling affects user experience and accessibility.

##### D. Organizing Styles

* Structure styles efficiently to keep code clean and maintainable.
* Separate styling logic from component logic where appropriate.
* Use centralized style management for consistency.

##### E. Platform-Specific Styling

* Implement styles tailored for different platforms (e.g., iOS and Android).
* Understand when and why platform-specific customization is necessary.

##### F. Using Icons

* Integrate icons to enhance UI clarity and aesthetics.
* Use icons to improve navigation and user interaction.

#### 3. Expected Outcomes

By the end of this section, you should be able to:

* Build visually polished user interfaces.
* Create reusable and scalable UI components.
* Apply and organize styling effectively.
* Implement platform-specific UI adjustments.
* Enhance applications using icons and visual elements.

#### 4. Summary

This section advances the application from functional to visually refined by focusing on styling, reusable components, and cross-platform design considerations.

*** 2- Borders ***
### Lecture Notes: Borders and Basic Styling in React Native

---

## 1. Cleaning Up the App Component

### Concept

* A parent `View` is used as a container.
* The container:

  * Fills the entire screen using `flex: 1`.
  * Centers all child components using:

    * `justifyContent: "center"` (vertical alignment)
    * `alignItems: "center"` (horizontal alignment)

### Step 1: Create a Centered Container

#### Code

-------------------code----------------------
import { View } from "react-native";

export default function App() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
    </View>
  );
}
-------------------code----------------------

---

## 2. Using Inline Styles (For Demonstration Only)

### Concept

* Inline styles are used here to keep all code visible in one place.
* In real applications, it is better to use the `StyleSheet` API for:

  * Better performance
  * Cleaner code
  * Easier maintenance

---

## 3. Creating a Styled Box

### Goal

Add a new `View` element styled as a blue square.

### Step 2: Add a Box with Background Color and Size

#### Code

-------------------code----------------------
import { View } from "react-native";

export default function App() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <View
        style={{
          backgroundColor: "dodgerblue",
          width: 100,
          height: 100,
        }}
      />
    </View>
  );
}
-------------------code----------------------

### Result

* Displays a blue square centered on the screen.

---

## 4. Adding Border Styles

### Concept

React Native allows customization of borders using:

* `borderWidth`
* `borderColor`
* `borderRadius`

---

### Step 3: Add Border Width and Color

#### Code

-------------------code----------------------
<View
  style={{
    backgroundColor: "dodgerblue",
    width: 100,
    height: 100,
    borderWidth: 10,
    borderColor: "royalblue",
  }}
/>
-------------------code----------------------

### Result

* A blue box with a thick royal blue border.

---

### Step 4: Add Rounded Corners

#### Code

-------------------code----------------------
<View
  style={{
    backgroundColor: "dodgerblue",
    width: 100,
    height: 100,
    borderWidth: 10,
    borderColor: "royalblue",
    borderRadius: 10,
  }}
/>
-------------------code----------------------

### Result

* The box now has rounded corners.

---

## 5. Customizing Individual Borders

### Concept

Borders can be customized individually for each side:

* `borderTopWidth`
* `borderTopLeftRadius`
* `borderTopRightRadius`
* `borderBottomLeftRadius`
* `borderBottomRightRadius`

---

### Step 5: Make the Top Border Thicker

#### Code

-------------------code----------------------
<View
  style={{
    backgroundColor: "dodgerblue",
    width: 100,
    height: 100,
    borderWidth: 10,
    borderColor: "royalblue",
    borderRadius: 10,
    borderTopWidth: 20,
  }}
/>
-------------------code----------------------

### Result

* The top border becomes thicker than the other sides.

---

### Step 6: Modify Only One Corner Radius

#### Code

-------------------code----------------------
<View
  style={{
    backgroundColor: "dodgerblue",
    width: 100,
    height: 100,
    borderWidth: 10,
    borderColor: "royalblue",
    borderTopWidth: 20,
    borderTopLeftRadius: 50,
  }}
/>
-------------------code----------------------

### Result

* The top-left corner becomes heavily rounded.
* Each corner can be styled independently.

---

## 6. Creating a Perfect Circle

### Concept

To create a circle:

* `borderRadius` must be at least half of width and height.
* If width and height are 100, radius must be at least 50.

---

### Step 7: Convert Box into Circle

#### Code

-------------------code----------------------
<View
  style={{
    backgroundColor: "dodgerblue",
    width: 100,
    height: 100,
    borderWidth: 10,
    borderColor: "royalblue",
    borderRadius: 50,
  }}
/>
-------------------code----------------------

### Result

* The square becomes a perfect circle.

---

## 7. Important Notes

### Independent Pixels

* React Native uses independent pixels instead of device pixels.
* Ensures consistent appearance across different screen densities.

### Best Practice Reminder

* Inline styles are used only for learning.
* Production apps should use `StyleSheet.create()`.

---

# Full Final Code

-------------------code----------------------
import { View } from "react-native";

export default function App() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <View
        style={{
          backgroundColor: "dodgerblue",
          width: 100,
          height: 100,
          borderWidth: 10,
          borderColor: "royalblue",
          borderRadius: 50,
        }}
      />
    </View>
  );
}
-------------------code----------------------

---

## Summary

This lecture introduced:

* Centering content using Flexbox.
* Using inline styles for demonstration.
* Creating UI elements with background color and size.
* Applying border styling and customization.
* Creating circles using border radius.
* Understanding independent pixels in React Native.

Next Topic: Implementing Shadows in React Native Styling.


*** 3- Shadows ***
### Lecture Notes: Shadows in React Native

---

## 1. Overview of Shadows

Shadows help create depth and visual hierarchy in user interfaces. In React Native, shadow implementation differs between iOS and Android.

* iOS supports detailed shadow customization.
* Android supports shadows using a single property called `elevation`.

In this lecture, styling continues using the same centered blue box.

---

## 2. Base Setup (Blue Box in Center)

### Step 1: Create the Container and Box

#### Code

-------------------code----------------------
import { View } from "react-native";

export default function App() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <View
        style={{
          backgroundColor: "dodgerblue",
          width: 100,
          height: 100,
          borderWidth: 10,
          borderColor: "royalblue",
          borderRadius: 50,
        }}
      />
    </View>
  );
}
-------------------code----------------------

---

# iOS Shadow Properties

To apply shadows on iOS, at least three properties must be used.

---

## 3. Shadow Color

### Concept

Defines the color of the shadow.

### Step 2: Add Shadow Color

#### Code

-------------------code----------------------
shadowColor: "gray",
-------------------code----------------------

---

## 4. Shadow Offset

### Concept

Controls the position or direction of the shadow.

It accepts an object with two properties:

* `width` moves the shadow horizontally
* `height` moves the shadow vertically

---

### Step 3: Add Shadow Offset

#### Code

-------------------code----------------------
shadowOffset: {
  width: 10,
  height: 10,
},
-------------------code----------------------

### Result

* Moves shadow diagonally to the bottom-right.

---

### Step 4: Modify Offset Position

#### Example 1: Move Shadow Vertically Only

-------------------code----------------------
shadowOffset: {
  width: 0,
  height: 10,
},
-------------------code----------------------

#### Example 2: Move Shadow to the Opposite Direction

-------------------code----------------------
shadowOffset: {
  width: -10,
  height: 10,
},
-------------------code----------------------

---

## 5. Shadow Opacity

### Concept

Controls how dark or transparent the shadow appears.

* Value ranges from `0` to `1`
* `0` means invisible
* `1` means fully visible

---

### Step 5: Add Shadow Opacity

#### Code

-------------------code----------------------
shadowOpacity: 0.5,
-------------------code----------------------

---

## 6. Shadow Radius

### Concept

Controls the softness or blur of the shadow edges.

---

### Step 6: Add Shadow Radius

#### Code

-------------------code----------------------
shadowRadius: 10,
-------------------code----------------------

### Result

* Creates a softer and more natural shadow.

---

## 7. Full iOS Shadow Example

#### Code

-------------------code----------------------
<View
  style={{
    backgroundColor: "dodgerblue",
    width: 100,
    height: 100,
    borderWidth: 10,
    borderColor: "royalblue",
    borderRadius: 50,
    shadowColor: "gray",
    shadowOffset: {
      width: 0,
      height: 10,
    },
    shadowOpacity: 0.5,
    shadowRadius: 10,
  }}
/>
-------------------code----------------------

---

# Android Shadow Property

Android handles shadows differently and provides limited customization.

---

## 8. Elevation Property

### Concept

* `elevation` adds depth and shadow automatically.
* Accepts a numeric value.
* Higher values produce stronger shadows.
* Android does not support custom shadow color, offset, opacity, or radius.

---

### Step 7: Add Elevation

#### Code

-------------------code----------------------
elevation: 20,
-------------------code----------------------

---

## 9. Full Android Shadow Example

#### Code

-------------------code----------------------
<View
  style={{
    backgroundColor: "dodgerblue",
    width: 100,
    height: 100,
    borderWidth: 10,
    borderColor: "royalblue",
    borderRadius: 50,
    elevation: 20,
  }}
/>
-------------------code----------------------

---

# 10. Cross-Platform Shadow Example

To support both iOS and Android, combine all shadow properties.

---

## Step 8: Apply Cross-Platform Shadow

#### Code

-------------------code----------------------
import { View } from "react-native";

export default function App() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <View
        style={{
          backgroundColor: "dodgerblue",
          width: 100,
          height: 100,
          borderWidth: 10,
          borderColor: "royalblue",
          borderRadius: 50,

          // iOS Shadow
          shadowColor: "gray",
          shadowOffset: {
            width: 0,
            height: 10,
          },
          shadowOpacity: 0.5,
          shadowRadius: 10,

          // Android Shadow
          elevation: 20,
        }}
      />
    </View>
  );
}
-------------------code----------------------

---

## 11. Important Differences Between Platforms

### iOS

* Supports detailed shadow control:

  * Color
  * Offset
  * Opacity
  * Blur radius

### Android

* Supports only `elevation`
* Limited customization options
* Shadow appearance depends on system rendering

---

## 12. Best Practices

* Use combined styling to ensure cross-platform consistency.
* Test shadow appearance on both platforms.
* Use moderate shadow values to avoid visual clutter.

---

## Summary

This lecture introduced:

* Shadow styling in React Native.
* iOS shadow properties and customization.
* Android elevation-based shadow system.
* Cross-platform shadow implementation.
* Understanding visual depth using shadows.

Next Topic: Padding and Margin in React Native.
